# Project: Last Light (Tower Defense)

## Goal
Build a fully playable tower defense game with 10 maps, 6 tower types with tier branching, distinct alien enemy visuals, and a storyline — deployed at https://tower-defense-black.vercel.app

## Current Status (2026-09-20)
**The campaign is feature-complete and shipped. `main` is at `a3c717d` and every PR is merged — nothing is open.** `main` is the Vercel production branch, so all of the below is live.

**This session was a bug-hunt that turned into four merged PRs (#66, #67, #68, #69), plus hero systems landed in parallel by another session (#63, #64, #65).** Suite is now **84 files / 1168 tests**; build clean.

The bug hunt started from "damage doesn't register with enemies" and ended up finding three distinct defects, none of which the test suite could see:
- **#66 — every hit now visibly registers.** Damage was always applying (`computeDamage` floors at 1), but both feedback channels were gated on hit SIZE: the HP bar moved sub-pixel, and damage numbers are suppressed under 30. 19 of 90 tier-1 (tower, enemy, wave) combos showed literally nothing, worst case 38 hits per pixel. Added a magnitude-independent hit flash and a one-pixel floor on bar depletion.
- **#67 — towers upgraded mid-buff were permanently crippled.** `Tower.upgrade()` assigned `fireRate` directly, bypassing the mod stack, so `_baseFireRate` kept the PRE-upgrade value and clearing Overcharge reverted the tower for the rest of the run. Rapid Cannon ran at 0.45 instead of 1.2 (37%). Only cannon T4B and sniper T4B redefine `fireRate` — i.e. exactly the two branches bought for DPS.
- **#68 — every enemy's HP bar was drawn ON its own sprite.** Bar geometry came from `def.radius` (the Graphics fallback body) while sprites were scaled up 2–6x. A titan's 48px bar sat inside a 138px chest. This was the *actual* cause of the reported "takes 3-4 shots for an archer to damage a drone": the archer did a clean 15 every shot, but the bar was narrow, green-on-green over green drone art, so the player read the 50%/25% COLOUR flips — which on a 70 HP drone land on hits 3 and 4.

**#69 — hero XP progress bar** (the session's one feature): a gold bar under the hero HP bar, filling as the hero deals damage, with a hover tooltip and `· MAX` at the cap. Display-only — `src/data/` and `src/sim/` untouched, and `heroXpProgress()` delegates to the existing `heroLevelForDamage()` so the headless balance simulator cannot drift.

**Remaining work is the optional iOS port (#9, large)** plus one follow-up this session opened and did not do (backlog #15, armour legibility — see Loose ends). The most valuable next thing is still a **human playtest of the full campaign**; it has only ever been simulated.

## Blockers
- None active

## Known Bugs
- None active. Three shipped bugs were found and fixed this session (#66, #67, #68) — see Current Status. All three were invisible to a green suite and were caught by playing the game and by code review.
- _(Resolved 2026-06-18: "hero not blocking on Level 2" verified as NOT a bug at the time — the hero was a ranged auto-attacker by design. **Superseded 2026-09-20:** PR #63 made heroes block enemies, take melee damage and regenerate out of combat, so the hero now does block.)_

## In Progress
**Nothing in progress. No open PRs.**

Last shipped (2026-09-20), newest first:
- **PR #69 — hero XP progress bar.** Full pipeline: brainstorm → spec → plan → 7 tasks via subagents with two-stage review each → live verification → PR. Spec `docs/superpowers/specs/2026-09-20-hero-xp-bar-design.md`, plan `docs/superpowers/plans/2026-09-20-hero-xp-bar.md`.
- **PR #68 — enemy HP bar lifted off the sprite.** New pure `src/systems/hpBar.js` (`hpBarGeometry`) sizes and positions the bar from the RENDERED sprite, falling back to `def.radius` only when there is no art.
- **PR #67 — fire-rate revert fixed.** New `setBaseFireRate()` in `fireRateMods.js`; `Tower.upgrade()` routes tier changes through it.
- **PR #66 — hit feedback.** `Enemy.hitFlash` + `EntitySprite.setFlash()` (solid-white silhouette tint), and `hpBarFillWidth()` reserving one pixel of depletion.
- **PRs #63/#64/#65 — hero melee, regen and damage-levelling** (landed by a parallel session; #65 recovered #64's work after a stacked-PR merge-order slip).

### Hard-won facts worth not re-deriving
- **`main` IS the Vercel production branch.** Verified via the deployments API: only `main` produces `target: "production"`; every other branch is `target: null` (a preview). Do NOT infer the deploy target from `origin/HEAD` — that pointed at an integration branch and is what let the live site sit 141 commits stale for two months (fixed in PR #45).
- **Vite copies ONLY `publicDir` into `dist/`.** Art must live under `public/assets/...`. A repo-root `assets/` dir is served by the dev server and silently dropped from the build — that shipped broken backdrops for two months. `npm run assets` now detects this specific mistake.
- **Browser-verify the production build (`npm run build && npm run preview`), not just `npm run dev`.** The dev server serves files the build omits; every prior "browser-verified" note was localhost-only, which is why the 404s survived so long.
- **FLUX.1 [schnell] runs at CFG 0, so negative prompts are inert.** The positive prompt is the only lever. A quoted proper noun in a prompt gets *drawn*: `cohesive 'Last Light' art style` put "LAST LIGHT" poster text on 6 of 10 nodes (one misspelled "LAST LIGNT"). Regression-tested in `promptParser.test.js`.
- **Machine limits:** Apple M4 / 24 GB. FLUX-schnell q8p is 12.3 GB resident. Generating while Chrome holds ~6.5 GB exhausted swap (33.7/34.8 GB) and wedged Draw Things in an uninterruptible I/O wait — twice. Close Chrome and generate in small batches, or drop to a q5p/q6p quantization.
- **Sprite animation: derive frames from ONE reference, do not generate N frames.** Character consistency across an animation is the hard problem, and the cheapest win is to never have more than one creature — every frame is the same pixels under a transform, so drift is *impossible by construction*. The trade is that limbs do not articulate; at 27–66px a leg is 1–2px, so a walk cycle is sub-pixel noise while identity drift reads as obvious flicker. Pipeline: `npm run art -- --kind sprite` → `scripts/build-sprite-sheets.py --all` → manifest entry → `npm run assets`.
- **The SDXL + IP-Adapter/ControlNet route in PROMPTS.md is NOT installed and is a poor fit anyway.** Draw Things has only FLUX.1 schnell — no SDXL checkpoint, no ControlNet, no IP-Adapter, no ComfyUI (~15GB + an install to change that). More to the point **ControlNet-OpenPose is trained on human skeletons**, so it is meaningless for a four-legged chitin drone or a hovering wraith. Revisit only if entity art ever renders much larger.
- **rembg model matters: use `isnet-general-use`, not `u2net`.** isnet excludes the ground shadow the generator draws despite being told not to; u2net keeps it as a translucent grey blob welded to the subject's feet.
- **The generator scatters debris and secondary subjects, and the negative prompt cannot stop it** (FLUX at CFG 0 ignores `negative_prompt` outright — same root cause as the baked-in title text). The compositor runs a largest-connected-component pass: the titan arrived standing in a field of rocks and cropped 667x971 with the debris vs 392x874 without, which shrank the creature inside its cell.
- **The generator renders these creatures facing LEFT whatever the prompt says** — the compositor mirrors by default, since all sprites must be authored facing right.
- **A prompt bullet's name IS the entity's runtime `type`.** `getSpriteConfig(category, type)` matches on the runtime type, so a bullet named for the character rather than the id produces art that parses, composites, registers — and silently never renders, falling back to Graphics with no error anywhere. Three hero bullets used `dax`/`vex`/`mira` instead of the `HEROES` keys `engineer`/`scout`/`pyro`, and soldier/sentry emitted their own name where both entities construct with `type: 'default'`. Regression-tested in `promptParser.test.js`.
- **A fenced prompt must beat an inline backtick span.** `parseSpritePrompts` took the last bullet-line span over 15 chars as the prompt, so the three bullets that document their output path (`assets/sprites/heroes/`) had that PATH used as the prompt and their real art brief ignored. `hero_rael`, `soldier_default` and `sentry_default` were all about to be generated from a file path. Fixed in the parser, not by rewording the markdown.
- **A tower's tier is encoded ONLY in the `_bg` stroke width**, and `_bg` was hidden outright once sprite art went active — so dropping tower art would have made tier invisible on the board. It now draws the ring without the fill disc. Verified live: the ring visibly thickens from tier 1 to tier 4.
- **"Base disc" in a tower prompt is load-bearing.** The barracks was the only tower prompt without it and came back on a big square grass plot with "EILDY" stamped on the wall; adding the base disc and an explicit no-lettering clause fixed both in one roll.
- **The anti-emplacement clause fixes WHAT a unit is, not what it STANDS ON.** Saying "not a turret, not a building" made the soldier and pyro human but the generator still welded them to a platform, and largest-connected-component cannot strip that because figure and platform are one blob. The prompts now also say "standing on plain empty ground with nothing beneath him".
- **Judge sprites on a DARK background, not a light one.** A retained grey ground shadow is nearly invisible against pale terrain and obvious against dark. Only `rael` had one, and it was only caught by compositing a contact sheet over both.
- **Legibility is decided by contrast against the emplacement pad, not by size.** The archer first shipped as a flat tan ballista on a cream base disc — the same tone as the pad — and read as a muddy blob at play scale even though every measurement was correct. Re-prompted for a tall dark-iron mount on a compact dark base. Unlike the other failures this one is invisible to tests, to `npm run assets` and to the contact sheet; only the running game showed it.
- **Narrow towers need a bigger cell, exactly like the big enemies.** `_fit` scales the whole silhouette into the cell, so a tall narrow subject ends up short of the 36px tower footprint and needs upscaling. Mage (aspect 0.48) and archer (0.44) both sit at 96px; at 64 they needed 1.44x and 1.57x.
- **The generator faced these RIGHT, unlike the enemies.** Every creature came back facing left, so the compositor mirrors by default — but 11 of the 12 towers/heroes/units needed `--no-mirror`, and only the cannon and pyro took the default. Check each reference; do not assume.
- **The shared style anchor's "sci-fi tower-defense _unit_" biases toward emplacements.** Skitter came back a turret and phantom a monolith until their prompts said "living creature" explicitly. Towers want that phrasing; creatures do not. Relevant to #8(c)/(d).
- **Enemy facing: `setFacing` takes a PIXEL delta from enemies but a ±1 UNIT from Hero.** A deadzone belongs at the enemy call site, never inside `EntitySprite.setFacing` — an 8px deadzone there stops heroes turning at all. The bends that caused mirror-flipping are *real* 13–26px backtracks in the dense Catmull-Rom path, not sampling noise, which is why a longer look-ahead does nothing (measured: unchanged at 8/16/24px) and only ignoring small deltas works.
- **A one-shot state needs `frames > 1` AND a fully-transparent last frame.** No `animationcomplete` fires for a single-frame one-shot, so the entity is never destroyed; and a final frame with pixels still in it pops out of existence. `_fadeOutDeadEnemy` now waits for the animation — measured live at 3001/3016/3017/3017/3000 ms for a 6-frame 2fps sheet.
- **Cell size should scale with the creature, not be fixed at 64px.** A 64px cell left the titan 24px wide inside it, needing `scale: 2.2` and looking soft. Colossus ships at 96, titan at 128. Set `scale` from the measured silhouette width (~1.2x the `def.radius` footprint), not by eye.
- **Draw Things API:** Settings → "API Server" → **HTTP** (not gRPC — gRPC needs a protobuf client, which `npm run art` does not speak). It listened on 7860 in HTTP mode, 7859 in gRPC mode. Draw Things' own weights are in its proprietary format, NOT safetensors, so mflux/ComfyUI cannot reuse them.
- **`def.radius` is the Graphics FALLBACK body, not the art.** After the entity scale-up (PR #62) sprites are 2–6x the radius-derived box (drone 9 → 37.8, titan 22 → **138.2**). Anything positioned "above" an entity from `radius` lands ON the sprite — that is how every enemy's HP bar ended up inside its own body (PR #68). Read the real size via `EntitySprite.getDisplaySize()` and fall back to radius only when there is no art. **`Hero.js` and `Soldier.js` still size their HP bars from radius and have the same latent defect.**
- **Feedback gated on magnitude goes silent exactly when the player most needs it.** Flat armour (`max(1, dmg - armor)`) floors a tier-1 tower to 1 damage against brute/colossus/titan, and enemy HP scales +13%/wave while tower damage does not — so the silent band widens as a run goes on. Any new feedback channel needs a magnitude-INDEPENDENT component (the hit flash) or it will vanish in the cases that look like bugs.
- **`Tower.upgrade()` must not assign `fireRate` directly.** `applyFireRateMod` captures `_baseFireRate` once and never overwrites it, so a direct assignment during a buff leaves the capture stale and the tower reverts permanently when the buff clears. Route tier changes through `setBaseFireRate()`. Only cannon T4B and sniper T4B redefine `fireRate`, which is why this hid for so long.
- **`UIScene` is instantiated ONCE for the life of the game.** It is registered in `main.js`'s scene array and relaunched with `scene.launch('UIScene')`, so `create()` re-runs on the SAME JS instance. Any memoised state must be reset in `create()` or it leaks across maps. Caught in review, not by tests — every test does `Object.create(UIScene.prototype)` fresh.
- **CSSOM canonicalises whole-number percentages on read-back.** `el.style.width = '40.0%'` reads back `'40%'`, while `'38.4%'` is preserved. A test asserting `'40.0%'` can never pass against a real DOM no matter what the implementation does.
- **Test the boundary case that can actually fail.** A `startLevel: 3` clamp test passed even with the threshold index deliberately broken — the degenerate `needed: 0` tripped a zero-guard and returned the expected value anyway. Only `startLevel: 2` had teeth. **Mutation-check new tests**: revert the fix, confirm the test fails, restore.

19. `[review]` ✅ **Wave preview, codex, and hero ability tooltips — DONE, PR pending** _(2026-09-21)_. Three information features that surface data the game already had. **(a) Pre-wave composition popover** on hover/focus/tap of Send Wave: types, counts, HP/armour/ground-or-flying, and counters. **(b) Codex** (towers with full tier ladders, heroes, enemies) reachable from the map-select meta bar and mid-level; entries never encountered are dimmed and tagged but stay readable and clickable, because a codex that withholds strategy defeats its own purpose. **(c) Styled ability hover cards** with four states (available / cooldown / locked-by-level / locked-by-hero), on both the in-game HUD and a NEW ability strip in Hero Command, which previously showed abilities nowhere at all. New pure modules `src/systems/entityDescriptors.js`, `wavePreview.js`, `codexCatalog.js`; new DOM modules `src/ui/WavePreviewPopover.js`, `CodexOverlay.js`, `AbilityTooltip.js`, `matchupText.js`. **1336 tests (154 new), build clean, 32/32 live browser checks + 11/11 replay checks against the production build.**
    - **Encounter gating is DERIVED from existing save progress — no SaveManager migration.** Towers are never gated (all six are purchasable from wave 1, so dimming them would be a lie); heroes reuse `isHeroUnlocked`; enemies come from `MAP_WAVES` of every map reached (reached = unlocked, not cleared, so an enemy you are losing to is documented).
    - **Disabled buttons fire no pointer events in Chrome/Safari — this bit twice.** Once on Send Wave (solved with the `#wave-btn-wrap` wrapper) and again on the ability buttons, where `locked` and `on cooldown` both set `disabled`, making two of the card's four states unreachable in a real browser while jsdom happily passed. Fixed with the same wrapper pattern (`#ability-<slot>-wrap`) and verified live: `eDisabled=true shown=true`. **If you add a hover affordance to any control this codebase disables, wrap it.**
    - **Pause handshake proved behaviourally, not by assertion.** There is no `window` handle on the Phaser game and adding one just for a test would be production code serving the test, so the check watches lives tick down: 24→24 over 6s with the codex open, 24→23 after closing, and a deliberate pause survived a codex visit untouched.
    - **KNOWN GAP — no touch path to the wave preview.** On a touchscreen the Send Wave button is the only thing to tap, and tapping it sends the wave. Tap-toggle was removed because it opened the popover *and* fired the wave, leaving a stale preview over a running wave. Hover and keyboard work fully. Options if this matters: tap-to-preview-then-tap-to-send, or a separate ⓘ affordance — both are really part of the iOS port (#9). Ability cards have the same touch gap (`pointerenter` never fires from a tap).
    - **Two reviews per task earned their keep**, catching a prototype-chain crash, three balance-table aliasing bugs (one the plan itself introduced), a precedence test that tested nothing, an unfalsifiable teardown test, a matchup shape that would have been re-parsed in three separate files, and the Ice tower's per-tier slow silently missing from the codex — the one stat an ice build is chosen for.
    - **The plan's own wave literals were wrong**, read from the primary checkout while it sat 41 commits behind `origin/main`; two balance commits (`c134db2`, `18641a1`) had retuned map 0 since, moving brutes off wave index 3 entirely. Tests now derive balance expectations from the tables. See `docs/superpowers/specs/2026-09-21-*` and `docs/superpowers/plans/2026-09-21-*`.

## Loose ends
- **Armour legibility is the one thing raised this session and NOT done** — tracked as backlog #15. The player has no in-combat signal for WHY a hit is weak. 8 tower/tier/enemy combos deal exactly 1 damage a shot (ice T1–T3 and archer T1 vs brute/colossus/titan) = **5m50s to 38m51s to kill one wave-8 enemy**. You chose "make it legible, don't rebalance": surface the armour interaction in the tower panel, do not touch the formula. The enemy inspector already shows `Armor N`; the TOWER panel shows only the weakness matrix.
- **`Hero.js` and `Soldier.js` HP bars still derive geometry from `def.radius`** — the same defect fixed for enemies in PR #68. Deliberately left out of scope; the report was about enemies.
- **Concurrent sessions share the primary checkout and WILL destroy each other's work.** Mid-session another session ran `git checkout main` → `pull` → `checkout feat/hero-melee-and-regen` → `reset`, which wiped uncommitted work in `/Users/keithtimko/projects/tower-defense`. Recovered only because a patch had been backed up first. **Work in a git worktree, not the primary checkout**, and back up a patch before any branch operation there.
- **Stale merged branches want a sweep.** Now includes `fix/enemy-hit-feedback`, `fix/tower-firerate-upgrade`, `fix/enemy-hpbar-legibility`, `feat/hero-xp-bar`, `feat/hero-damage-leveling`, `feat/hero-melee-and-regen` on top of the earlier list (`balance/barracks-worth-its-cost`, `feat/colossus-waves`, `feat/enemy-sprite-art`, `feat/sim-barracks-soldiers`, `feat/tower-hero-sprite-art`, `feature/natural-fit-paths`, `feature/space-themed-backgrounds`). All contained in `main`; none deleted yet.
- **A worktree is still checked out** at `~/.config/superpowers/worktrees/tower-defense/fix-enemy-hit-feedback` (currently on `docs/session-notes-2026-09-20`). Remove with `git worktree remove` when done.
- **Generated references live in a gitignored `.art-cache/sprites/`** and are NOT committed. Re-running `npm run art -- --kind sprite` produces *different* creatures. Treat the committed PNGs as the source of truth.
- Long-standing uncommitted edits to `SESSION_NOTES.md` (auto-generated commit log) and an untracked `.claude/prompts/` sit in the primary checkout's working tree; both are session bookkeeping, not user data.

## Prioritized Backlog
<!-- Risk tiers: [auto] runner may build→test→PR unattended · [review] needs spec/plan approval · [human] you drive. See ~/projects/CLAUDE.md → Backlog Risk Tiers. Note: this is a static Phaser game (no DB/auth/payments) — items are [review] (features/design) or [auto] (localized fixes/chores). -->
1. `[review]` ✅ **Phase 8b — remaining SFX work** — DONE, **PR #38 merged** → `feature/map0-path-fit` (2026-06-20). Code derives branch/enemy-specific SFX keys with graceful fallback to the base sound (mirrors PR #37 overworld-art pattern): new pure `src/systems/sfxKeys.js` (`towerFireSfxKey(type,branch,registeredKeys)`, `enemyHitSfxKey(type,registeredKeys)`) returns the specific key if registered in `SFX_KEYS` else the base; wired at GameScene tower-fire + Enemy hit call sites. `SFX_KEYS` unchanged so every call falls back today (0 audio decode errors); the 16 future assets (10 tower-branch + 6 enemy-hit) are documented in `assets/audio/PROMPTS.md` and drop in later with zero code change. 9 helper tests; 750 total; build clean; browser-verified (live branch-A tower resolved `tower-fire-archer-A` once registered; enemy-hit fired in real combat).
2. `[review]` ✅ **Verify hero on Level 2** — DONE (2026-06-18), **not a bug**. Root cause: the hero is a path-positioned ranged auto-attacker (range ~45), never a hard-blocker; `_checkSoldierBlock` is soldier-only **by explicit design** (hero-path-restriction spec §"Hero does NOT block enemies", flagged out-of-scope). Live-verified on Level 2 with `window.__game` introspection: hero spawns on-path, detects enemies in range, attacks (timer resets), and deals damage (skitter 45→17 hp) as they pass — it just doesn't stop them. Hero attack/move/respawn also covered by passing unit tests. _Optional future feature (not scheduled):_ soldier-style hero blocking — would need `_checkSoldierBlock` to include the hero + enemy-vs-hero melee; its own spec/plan.
3. `[review]` ✅ **Responsive canvas** — DONE, **PR #36 merged** (2026-06-19). Switched Phaser to `Scale.FIT` at a fixed 1280×720 design res (`autoCenter: CENTER_BOTH`); world content stays aligned with zero reflow (constant internal coords), letterbox bars on off-aspect windows. New pure `src/systems/viewport.js` `gameToPageCss` converts game→CSS px for the tower panel (`GameScene`) + inspect panels (`InspectController`). `index.html` `#game` flex-centers the canvas (removed the `width/height:100%!important` override). 721 tests; build clean; browser-verified at 16:9 / wide / tall (clean letterbox, no clipping) + tower-panel alignment under FIT.
4. `[review]` ✅ **Overarching storyline + per-level mini-stories** — DONE (Phase 1), **PR #40 merged** (2026-06-21). *Last Light* campaign: Sol Vanguard vs. the Vorn hive, Commander Rael, turning point at Titan's Reach, finale at the homeworld. New multi-panel story dialog (`src/ui/StoryDialogOverlay.js` + pure `src/systems/storySequence.js`) plays an opener, per-map pre-battle briefing + post-battle epilogue, boss beats, and an ending; content + helpers in `src/data/story.js`; mid-wave banner retained (rewritten). Once-then-replayable via SaveManager v3→v4 `seenStoryBeats` + a 📖 Story Log on MapSelect. Portraits use deferred-asset fallback (`src/systems/portraitFallback.js`). 786 tests; build clean; browser-verified. **Phase 2 (see #10):** real portrait art + cinematic opener/ending.
5. `[review]` ✅ **Map-progression overworld for MapSelect** — DONE, **PR #37 merged** (2026-06-20). Sidebar list replaced with a winding-path node graph: per-map `overworldPos`/`overworldArt` in maps.js, pure `src/systems/overworldState.js` `classifyOverworld` (completed/next/unlocked/locked + isFinal), `MapSelectScene._populateOverworld` renders SVG connectors + node `<img>` art with numbered-circle fallback. 741 tests; build clean; browser-verified (states, fallback, node-select→featured, PLAY launch, locked inert). **✅ Node art COMPLETE 2026-08-20 (PR #52)** — all 10 `overworld_*.png` generated locally with FLUX.1 [schnell] via the Draw Things HTTP API, reviewed individually for text artefacts, downscaled to 512×512, and serving 200 in production. The numbered-circle fallback is now dead code in practice but retained.
6. `[review]` ✅ **Show tower range before placing** — DONE, **PR #33 merged** (2026-06-16). Range ring at cursor while a tower type is armed, snaps to nearest slot, gold when affordable. `src/systems/rangePreview.js` + `GameScene._drawZones`.
7. `[review]` ✅ **Calibrate per-level economy** — DONE, **PR #34 merged** (2026-06-17). Retuned per-map `startGold` to a ~2-tower opening hand + new per-map `rewardMult` scalar (in `maps.js`) that uniformly scales kill payouts (via new `_killReward` helper), wave-clear bonus (`WAVE_CLEAR_BONUS` const), and early-send bonus in `GameScene`. Full cheap board now first affordable ~75% through a level vs. wave 3 today. **Calibration values are model-derived starting points — needs human playtest-tuning for winnability per map.** _Follow-up 2026-08-18 (PR #48): built a headless balance simulator + model-free economy diagnostics (`npm run balance`). **Exact finding (no modelling involved): every map's total gold ceiling is only 1.32–1.75x the cost of filling its slots with the cheapest tower**, and gold-per-enemy-HP falls ~4x from map 0 (0.0549) to map 9 (0.0139). The simulated tower-only defence loses every map, which most likely means the campaign currently DEPENDS on the hero rather than that the maps are broken — the sim excludes hero/soldiers/sentries/abilities/meta-upgrades. **Next step to make the sim a tuning instrument: model the hero** (see #11)._
8. `[review]` **Production-quality entity art** — decomposed into (a) infrastructure → (b) enemies → (c) towers → (d) heroes/soldiers/sentries.
   - **(a) sprite-rendering infrastructure — ✅ DONE, PR #42 MERGED** → `feature/phase-3-tower-system` (branch `feature/entity-sprite-infra`, 2026-06-24). Deferred-asset infra behind a Graphics fallback (mirrors sfxKeys/portraitFallback/overworld): pure `src/systems/spriteKeys.js`, declarative empty `src/data/sprites.js` manifest + `getSpriteConfig`, `src/systems/EntitySprite.js` (Phaser Sprite when art registered else Graphics fallback; idle/move/attack/death state machine + flipX facing), `BootScene` 404-tolerant loader → `registry.spriteKeys`. All 5 entities own an EntitySprite; **removed the redundant double-render** (`GameScene._drawTowers/_drawEnemies/_drawProjectiles` deleted — entity Containers are the single source; `gfx` keeps path/zones/particles). Enemy status rings split `_body`→`_overlay`. **Entity depths raised to a 12–16 band** (browser-verify caught: entities sat below the depth-10 static build-pad layer once the depth-30 gfx copies were gone → towers hid behind pads). 806 tests; build clean; browser-verified (map 0 + map 5) + light-up proof. Art lights up via 1 manifest entry + PNG (`assets/sprites/PROMPTS.md`).
   - **(b) enemy sprite art — ✅ DONE 2026-09-19, PR OPEN** (branch `feat/enemy-sprite-art`). All six enemies now ship a looping `move` + one-shot `death` sheet; **35/35 assets**, 968 tests, build clean. **Pipeline is NOT the SDXL + IP-Adapter/ControlNet route PROMPTS.md described** — none of it is installed (only FLUX.1 schnell), and OpenPose is trained on human skeletons so it is meaningless for a four-legged chitin drone. Instead: one generated reference per enemy → `rembg` (`isnet-general-use`, the model that drops the generator's ground shadow) → largest-connected-component isolation → mirror to face right → frames derived by transform (bob/rock for move; collapse + eased value-noise dissolve for death). **Character consistency is by construction — every frame is the same pixels**, so drift is impossible. **The trade: limbs do not articulate**; at 27–66px a leg is 1–2px, so a walk cycle is sub-pixel noise while identity drift would read as flicker. New: `scripts/build-sprite-sheets.py`, `npm run art -- --kind sprite`, `parseSpritePrompts`. Two code fixes fell out (see below). _(added 2026-06-24)_
     - **Facing deadzone** (`src/systems/facing.js`): the dense path genuinely backtracks 13–26px at hairpins, so sprites mirrored mid-stride (map 6: 11 flips per crossing → 1). A longer look-ahead does **not** fix it — only ignoring small deltas does. Deadzone lives at the enemy call site because Hero passes ±1 units to the same `setFacing`.
     - **Death destroy-delay** wired in `_fadeOutDeadEnemy` — enemies with death art play the one-shot and destroy on `animationcomplete`; everything else keeps the alpha fade. Measured live at 3001/3016/3017/3017/3000 ms for a 6-frame 2fps animation.
     - **Found: `colossus` is spawned by NO wave in any map** (`MAP_WAVES` has zero entries). Its art ships and is correct, but it is unreachable in normal play — dead content, worth a design decision. Verified only via a temporary local wave patch.
     - **Prompt gotcha:** the shared style anchor says "sci-fi tower-defense **unit**", which biases the generator toward emplacements — skitter came back a turret and phantom a monolith until their prompts said "living creature" explicitly.
   - **(c) tower sprite art — ✅ DONE 2026-09-19** (branch `feat/tower-hero-sprite-art`). All six towers ship a static `idle` plus a 5-frame firing beat; the barracks is `idle` only because it never fires. Emplacements do not breathe, so the idle is a single frame — legal to revert to, since only a single-frame *one-shot* is broken.
   - **(d) hero/soldier/sentry sprite art — ✅ DONE 2026-09-19** (same branch). Four heroes get `idle` + `move` + `attack`; the barracks soldier and engineer sentry get `idle` + `attack` at a 48px cell. **No `death` art** — the reserved destroy-delay wiring is deliberately untouched, since soldiers die and respawn continuously and a dissolve on each would clutter the board.
     - **Two code gaps the backlog never mentioned, both closed:** towers never called `setFacing`, so a turret authored muzzle-right always pointed right (no deadzone needed, unlike the enemy path — a tower's target jumps between discrete enemies); and the soldier was the only entity whose `attack` state was wired to nothing, so its attack art would never have played.
     - **Tier readability:** `_bg` is no longer hidden under sprite art — it draws the tier ring without its fill disc, because tier is encoded only in that stroke width.
     - **Compositor is now category-aware** (`OUT_DIRS`/`PLAN`/`STATE_OVERRIDES`/`CELL_OVERRIDES`/`HOVER`, `(category, type)`-keyed `TINTS`) with three new transforms: `build_idle_static`, `build_idle_breathe` and `build_attack`. The refactor was proven behaviour-preserving by rebuilding the five cached enemies **byte-identical** to the committed sheets.
     - **Verified in the browser on the production build**, not just tests: 39/39 sheets serve 200 from `dist/`; all five categories report `spriteActive: true` with the Graphics body hidden; facing flips with target side; every tower, whenever not animating, shows its `*-idle` texture on frame `__BASE` (nothing stuck on an attack frame); the tier ring visibly thickens tier 1 → tier 4.
11. `[auto]` ✅ **Balance simulator — model the hero** — DONE 2026-08-18 (PR #49). Hero auto-attack modelled (nearest-in-range, reusing `HEROES` + shared `computeDamage`); `pointAtProgress`/`pathLength` extracted to `pathGeometry.js` and `Hero.setPathPosition` now uses them, so hero and sim position identically. Hero is worth ~2-3 extra waves but does NOT make any map winnable alone. **New `findWinMultiplier` probe converts "unwinnable" into a number: the uniform damage multiplier at which each map clears.** Result: maps 0-6 ramp smoothly 1.25x -> 1.96x, then **map 7 jumps to 3.63x and maps 8-9 sit at ~4.3x — a difficulty cliff between map 6 and map 7**. Cross-confirmed by the independent economy table (gold-per-HP falls ~4x across the campaign): the last three maps give less gold AND demand more damage. _Original entry:_ `src/sim/` currently simulates towers + tier upgrades only, and reports every map as unwinnable. The hero is an always-present auto-attacker, so the model is missing a first-order damage source; until it is included the sim can't produce trustworthy `startGold`/`rewardMult` numbers. The economy diagnostics in `src/sim/economy.js` are exact and usable today regardless. Extend `simulateMap` with a hero walking the path and auto-attacking, reusing `HEROES` + the shared `computeDamage`.
12. `[review]` ✅ **Act on the balance finding** — DONE + SHIPPED 2026-08-21 (PRs #53 and #54, both merged). Step 1: barracks soldiers + their blocking are modelled in the simulator (#53), which showed a bought barracks was a NET LOSS on 9/10 maps. Step 2: fixed the barracks (#54) — root cause was a duty cycle (squad uptime 2.25s vs a 3s respawn), not a weak stat; `respawnDuration` 3→2 (1.5→1 on tier4B) and `ENEMY_MELEE_DAMAGE` 20→12, moved to `src/data/enemies.js`. A barracks now helps on all ten maps. **Decision 2026-08-21: the map-7 cliff STAYS — accepted as intentional difficulty, `maps.js` is NOT being retuned.** **Re-affirmed 2026-09-19** after the colossus swap (#14) was measured to narrow it: map 7 is now held out of that swap and sits at its original **3.63x / 3.08x / 2.80x**, guarded by two tests in `waves.test.js`. `maps.js` still untouched. The cliff is a four-dial step at map 7 (HP +30%, lives −20%, rewardMult 0.30→0.25, phantoms 71→131 becoming the plurality enemy); it survives every model variant, so it is a design choice, not a modelling gap. _Original entry:_ `npm run balance` reports a difficulty cliff: the damage multiplier a modelled defence needs to clear each map ramps smoothly 1.25x→1.96x across maps 0–6, then jumps to **3.63x at map 7 and ~4.3x at maps 8–9**. Independently, the model-free economy table shows gold-per-enemy-HP falling ~4x across the campaign — the last three maps pay less AND demand more. Two options: (a) retune maps 7–9 (`rewardMult`/`startGold`/wave tables), or (b) first model barracks soldiers — their *blocking* is the largest unmodelled factor and could well explain the early-map gap, which would change what "needs fixing" means. **Recommend (b) before (a).**
13. `[auto]` ✅ **Overworld node contrast polish** — DONE 2026-09-20, **PR #60 open**. Art-only, no code change.
    - **Root cause was the prompts, not the generation.** Every other node names a lit subject against dark space; 6 and 7 named the dark space itself ("faint guide-lights ... deep indigo", "an ominous deep-purple void ... a faint unseen threat"). The generator rendered exactly that. Raising brightness would not fix a node with nothing in it.
    - **Subjects taken from each map's own briefing.** Map 6: *"Every comm relay ahead is dark."* → derelict relay pylons receding toward a lit beacon. Map 7: *"Multiple Titan-class contacts. This close to home, they travel in packs."* → a pack of armoured chitinous leviathans in the violet dark.
    - **Measured** (512px, mean / p90 / %above-mid-grey): node 6 **27.4/57/1.50% → 44.8/84/3.24%**; node 7 **26.5/43/0.39% → 62.1/118/8.22%**. Node 7 now above the set median (55.7). Both hold a silhouette under the locked `grayscale(1) brightness(0.4)`.
    - **Two corrections to this item's original description:** nodes render at **46px, not 64px**; and 6/7 were **not** the two darkest — **node 3 (Orbital Station) is, at mean 17.7 / p90 33**. Node 3 was left alone deliberately: its prompt asks for a lit station and delivered one, and its cyan ring reads as distinctive at size, where 6 and 7 had no subject at all. Revisit if it bothers you.
    - **Known trade:** node 6 reads more as a ridgeline than a star-lane, drifting slightly from "Deep Space Corridor". Invisible at 46px, so accepted rather than re-rolled.
14. `[review]` ✅ **Colossus reaches the campaign** — DONE + SHIPPED, **PR #58 merged** as `27341d1` (2026-09-20). Closes the dead-content decision PR #56 left open, and the phantom-scale one with it.
    - **Not a boss.** 400hp / armour 15 / reward 55 is exactly half a titan (800/20/80) and sits between the brute (120/8) and it. The ladder had a 6.7x HP jump straight from brute to titan; colossus is that missing rung. The titan is not a one-per-level boss either — it scales by count (1 at map 5 w9 up to 6 at map 9 w17).
    - **Debut on map 4** (w8 x1, w11 x2, w13 x2). Map 4 was the ONLY map between two introductions that taught nothing new — drone/skitter/brute debut on map 0, phantom on map 3, titan on map 5 — and it sits one map before the titan, so colossus reads as the armour check that prepares you for them.
    - **Maps 5, 6, 8, 9: recurring, paid out of the titan budget.** 1 titan → 2 colossus is exactly HP-neutral; verified at **0 HP delta** on every swapped map (map 4 takes +2000 HP, the debut). **Map 7 is deliberately held out** — see the cliff entry below.
    - **Measured, not assumed — HP-neutral is NOT difficulty-neutral, and it cuts BOTH ways.** Full A/B re-measured 2026-09-19 across three states (no colossus / swap everywhere / swap minus map 7). Two opposed mechanisms, not one:
      - **Favours the defender:** armour is *flat subtraction* (`max(1, dmg - armor)` in `src/systems/damage.js`), so trading armour 20 for 15 multiplies a tower's throughput by **(d-15)/(d-20)** — 1.25x for a tower hitting 40, **2x** at 25, and up to **5x** at or under 20, where the titan takes the floor of 1. The ratio is steepest exactly where per-hit damage is lowest. Plus 2x55 reward beats 1x80: **+30 gold per swap**.
      - **Favours the attacker:** `lives--` is per *leaked enemy* (`src/sim/simulate.js:265`), so a swap that leaks costs **2 lives instead of 1**.
      - Which wins depends on whether the map's defence is killing the heavies or leaking them. **Maps 4, 5, 6 got HARDER** (no-barracks 1.60→1.68, 1.82→1.88, 1.96→2.01) — the life cost dominates. **Maps 8, 9 did not move at all** in the no-barracks column (4.28x, 4.34x both states); only their bought/free columns ease (8: 4.25→4.17, 9: 4.23→4.06). **Map 7 was the sole map whose no-barracks number moved**, and it moved a lot.
    - **✅ RESOLVED 2026-09-19 — map 7 is held out of the swap, restoring #12's cliff exactly.** The swap had moved no-barracks map 7 **3.63x → 3.35x** and bought **3.08x → 2.94x**, narrowing the map6→map7 step from +1.67 to +1.34. Reverting that single wave line (`src/data/waves.js:122`, `titan 3` restored in place of `titan 2 + colossus 2`) returns map 7 to **3.63x / 3.08x / 2.80x** — byte-identical to the pre-colossus baseline in **all three columns**. Two regression tests now encode the decision, so the next HP-neutral swap cannot silently re-narrow it.
      - **One caveat the original note got wrong:** reverting map 7 restores *map 7* exactly, but not the *step*, which has two ends. Map 6 is still carrying its own swap and got slightly harder (1.96x → 2.01x), so the step lands at **+1.62**, not +1.67. That residual moves in the direction #12 wanted (a sharper ramp into the cliff), so it is accepted, not chased.
      - **Cost of holding map 7 out:** none to the colossus's campaign presence — it still appears on maps **4, 5, 6, 8, 9**, five of ten, including its debut. The `spawns every type declared in ENEMY_DEFS` guard stays green.
    - **The suite had encoded the bug.** `VALID_TYPES` in `waves.test.js` was a hardcoded list omitting `colossus`, and every map asserted `contains no colossus enemies` — the tests actively *required* a defined, art-complete enemy never to be spawned. `VALID_TYPES` is now derived from `ENEMY_DEFS`, plus a new test that every declared enemy reaches some wave table.
    - **Phantom scale STAYS at 0.58** (decided 2026-09-19). It renders at 1.77x its hitbox where the other five enemies sit in a tight 1.28-1.35x band, but its bounding box is mostly trailing tendril and translucent wisp that the solid core does not fill, and it is the plurality enemy on maps 7-9 where a fast flyer has to stay readable. Do not "fix" this to 0.43.
15. `[review]` ✅ **Make `greedyBuildPlan` matchup-aware — DONE, PR pending** _(2026-09-22)_. Both the purchase pass and the upgrade pass now score candidates through the real `computeDamage` against the HP-weighted mix of enemies STILL TO COME, so armour, the weakness matrix and pierce all count. Model-only: nothing under `src/data/`, `src/scenes/`, `src/entities/` or `src/ui/` was touched.
    - **The old entry's evidence was stale; the defect was not.** "Five monotonicity inversions in eleven" re-measured as **two** (PR #71 retuned maps 0-2 after that note was written), and "the sim cannot tune any map past map 1" no longer held — maps 0-2 all reported OK. The real defect is far starker: `towerValue` ranked the **archer first**, and the archer lands **1.0 DPS** on a colossus and a titan while the sniper, ranked third, lands **36**.
    - **What the blind spot was actually worth** (new `Matchup sensitivity` A/B column, kept for one release): map 3 −0.03x, map 5 −0.08x, map 7 −0.36x, map 8 −0.41x, map 9 −0.76x, and **zero** on maps 0, 1, 2, 4, 6. It cost almost nothing on maps 3-6 and a great deal on 7-9, exactly where titans and phantoms dominate the HP budget.
    - **Maps 3-9 remain UNWINNABLE even with the aware model**, so the blind spot explains only part of the gap — the rest is real difficulty or the remaining omissions. That separation is what #16 was blocked on.
    - **Map 2 is the headline.** It went from keeping **30%** of lives to **85%** — the largest behavioural change on the branch — and `findWinMultiplier` saturates at 1.0, so the multiplier column showed `1x → 1x` and hid it entirely. The report now prints lives kept beside each multiplier.
    - **The A/B is trustworthy**: the final review proved the `blind` column reproduces pre-branch behaviour exactly — 120 runs across 10 maps × 6 damage multipliers × 2 barracks targets, **zero differences**.
    - **Five plan-level test defects were caught by the review loop**, all the same species — tests that passed while proving nothing: a DPS tie mistaken for an ordering (archer and sniper both do exactly 18 to skitters), a post-increment wave index, a missing exact-value pin that let a slice-relative bug through, and a ranking test that a forgot-to-divide-by-cost bug would have passed by alphabetical accident.

15-OLD. `[review]` ~~**Make `greedyBuildPlan` matchup-aware**~~ — `towerValue` in `src/sim/buildPolicy.js` is `damage * fireRate / cost`, which ignores BOTH flat armour and the weakness matrix. It is why the balance simulator cannot be trusted to tune any map past map 1: sweeping `startGold` in 5g steps on map 2 gives **five monotonicity inversions in eleven** (more gold, worse simulated outcome), because an 11-slot tier-3 map compounds the mis-ranking. Map 0 by contrast sweeps near-monotonically (1 inversion in 14). Until this lands, every per-map number beyond the on-ramp is a guess dressed as a measurement. Note the same blind spot traps *players*: `damage * fireRate / cost` ranks the archer best, and the archer does 5 DPS to a brute. _(raised 2026-09-20 by the on-ramp recalibration)_
16. `[review]` ✅ **Re-examine maps 3-9 difficulty — DONE, PR pending** _(2026-09-22)_. **The answer was not "the waves are too hard" — it was a campaign-wide economy squeeze, and the binding constraint was WHEN gold arrives, not how much.** `startGold` FELL 130→100 across ten maps while the board doubled (600→1200) and enemy HP rose 8.3x, so map 9 opened with **one tower on twenty slots** and the simulator died on wave 1 having built nothing. Only maps 0 and 2 had any recorded tuning rationale; maps 3-9's values had no derivation anywhere, and maps 6-9 violated PR #34's own stated "~2-tower opening hand" rule.
    - **The metric that exposed it.** `cheapestFullBoardCost` counts only the cheapest tower per slot and therefore **cannot see tier 4** — an archer costs 110 taken to tier 2 but **310** taken to tier 4. Measured against a fully-upgraded board, purchasing power collapsed **2.4x**: 0.75-0.79 on maps 0-1 against 0.26-0.34 on maps 4-9. `depthBoardCost` now lives in `src/sim/economy.js` and prints in `npm run balance`, so the next person sees it.
    - **Timing, not total — proven.** Holding total gold roughly constant and moving it earlier improved every map, and on maps 6 and 9 *less* total gold delivered earlier was strictly better (1581→1457 with 1.88x→1.52x; 2048→1946 with 2.89x→2.50x).
    - **Result:** maps 3/4/5 UNWINNABLE(1.19/1.57/1.52x) → **OK**, keeping 61%/56%/60% of lives; maps 6-9 1.88/2.23/2.80/2.89x → **1.05/1.30/1.36/1.36x**. Maps 0-2 untouched per #18, and pinned by a guard so a future sweep cannot erode that decision.
    - **The A/B check from #15 passed and then some:** aware ≥ blind on every map, and the gap WIDENED late (map 8 −0.41x → −1.06x). With a workable economy, matchup-aware tower choice matters MORE — evidence the tuning is not exploiting a model artefact.
    - **The final review caught two things the per-task reviews structurally could not.** (a) The derived rule pegs gold to the BOARD and never to the THREAT, so map 5 — more slots but less HP than map 4 — was over-funded and *created* a difficulty inversion: gold-per-HP went 0.0579 → **0.0640** → 0.0492, the only break in an otherwise clean decline. Fixed by a recorded per-map override (`rewardMult` 0.57→0.51). (b) The guard test **protected nothing** — a flat 120g campaign passed all five assertions while leaving map 9 worse than the state being fixed. Now guarded by a board-scaled floor and a gold-per-HP monotonicity assertion, both proven to fail against the exact regressions they name.

16-OLD. `[review]` ~~**Re-examine maps 3-9 difficulty**~~ _(#15 landed 2026-09-22)_. Start from the new `Matchup sensitivity` A/B column, and note four things #15 established before re-tuning anything:
    - **Map 2's provisional cell must be re-derived.** `src/data/maps.js:70-79` marks `startGold: 170 / rewardMult: 0.40` PROVISIONAL *because* the blind model could not resolve the map. Under the aware model it keeps **85%** of lives — just under the 90% TOO EASY ceiling. That comment is also now doubly stale: it cites the "five monotonicity inversions" figure (re-measured as two) and "towerValue ignores armour" (fixed).
    - **The map-2 startGold sweep did NOT become monotonic.** A hard flip survives (150g wins with 5 lives, 155g loses outright) and on a lives-sensitive count inversions went 2→3. Reported deliberately rather than tuned away. But see the next point before blaming the map.
    - **The A/B column bundles two changes** — tower choice AND upgrade ordering (cheapest-first → marginal damage per gold). The second concentrates gold into fewer towers (map 2: seven towers at T2 blind vs one mage at T3 aware), so the surviving non-monotonicity may be a property of concentration rather than of the map. Not separable from the current table.
    - **The model still picks slots blind** (see #27), so a sniper it now correctly buys may land in a slot ranked for a 120-range tower.
    _(original entry:)_ **Re-examine maps 3-9 difficulty** — `npm run balance` calls 7 of 10 maps unwinnable, but it also plays badly (see #15). Establish whether that is a real calibration problem or an artefact of the model, **after** #15 lands. Also closes the pre-existing **map 2 -> map 3 HP-per-wave inversion** (917 vs 898) that the on-ramp change shrank from 14.5% but did not remove — the `on-ramp difficulty ramp` guard in `waves.test.js` deliberately stops at map 2 for this reason. _(raised 2026-09-20 by the on-ramp recalibration)_
17. `[review]` ✅ **Make the armour floor LEGIBLE — DONE, PR pending** _(2026-09-21)_. Two surfaces: a `🛡 Armour absorbs:` line on the tower panel (absorbed **percentage**, `⚠` on rows floored to 1) and in-world damage numbers for absorbed hits the `THRESHOLD = 30` gate used to hide, muted and shield-marked, throttled per enemy. Formula untouched — `npm run balance` byte-identical to `origin/main`, nothing under `src/data/` or `src/sim/` touched.
    - **`pierce` is load-bearing and the backlog entry missed it.** `mage` and `sniper` both set `pierce: true`, so armour never applies to them. A first pass that ignored it produced **eight false warnings**. The flagged set is exactly archer, ice and barracks soldiers.
    - **The "eight combinations" undercounted.** It read only `TOWER_DEFS[t].damage` and missed `soldierStats`: a tier-1 barracks soldier deals 20 into a titan's armour 20 and floors to 1 — a **ninth** case, the worst in the set at 95%, on the tower most often bought as an opener.
    - **Absorption is measured on the armour step ALONE**, never `final / raw` — that conflates armour with the weakness matrix and would report "armour absorbed 50%" for a cannon hitting an unarmoured phantom.
    - **The final review caught what per-task reviews structurally could not.** The panel first printed `damage→after`, the mid-formula value — archer T2 read `Titan 25→5` while the hit deals 2. And the band was computed for EVERY `takeDamage`, so Pyro's 3dps burn printed `🛡1` on every armoured enemy once a second and evicted the tower signal through the shared throttle. Now gated to `source.kind === 'tower'`.
    - **Three tests were caught passing for the wrong reason** — two pierce cases whose numbers landed in the same band either way, and four throttle assertions blinded by synchronous pool recycling. Each fix was proven by deliberately breaking the code and observing the failure.
    - _(superseded description)_ **Make the armour floor LEGIBLE (do not rebalance it)** — `computeDamage` is `max(1, amount - armor)`, so any tower whose raw damage is at or below an enemy's armour lands **exactly 1 damage a shot**. Eight combinations floor out (ice T1 vs brute/colossus/titan, ice T2 vs colossus/titan, ice T3 vs titan, archer T1 vs colossus/titan) — 245 to 1632 shots, i.e. 5m50s to 38m51s, to kill one wave-8 enemy. Not a bug (measured across all 150 tower x enemy combinations: no NaN, no zero damage), but it reads to a player as a broken game. The enemy inspector shows `Armor N` while the TOWER panel shows only the weakness matrix, so "my damage is absorbed" and "this tower is broken" look identical. **Decision on file 2026-09-20: fix the legibility, not the formula.** Do NOT change the floor to a percentage or switch armour to proportional reduction without an explicit new decision — either shifts every map's difficulty including the deliberately-preserved map-7 cliff. _(specified 2026-09-20; was recorded in auto-memory as "#15" but never actually written to this backlog — the on-ramp work found the gap. Same root cause as that work's archer/brute trap, seen from the player's side rather than the tuner's.)_
18. `[auto]` ✅ **Enemy duplication on a replayed level — FIXED, PR #72.** `GameScene.create()` registered five listeners on `this.events` and `shutdown()` removed none; Phaser's `Systems.shutdown()` clears only its own `TRANSITION_*` listeners, so the scene emitter carried each one into the next `create()`. `WaveManager` emits `enemy:spawn` once per queued enemy, so the Nth entry into a level spawned **N enemies per spawn** — map 0 wave 1 went 6 → 12 → 18 → 24. **The feedback loop ran backwards: retrying a map you were stuck on made it harder**, which is most likely what made Level 1 feel unbeatable. Fixed with a matched `_wireSceneEvents`/`_unwireSceneEvents` pair, a named `_onHeroLevelUp` (an inline arrow can never be `off()`d), and `once('shutdown')` in all three scenes that used `on`. _(reported from play 2026-09-21)_
    - **DECISION 2026-09-21: the #71 on-ramp calibration STANDS. No further recalibration.** The simulator always built a fresh `WaveManager`, so its numbers were correct all along — it was the game that diverged on replays. With #72 in, real play matches the model, which is the condition #71 was tuned for. Do not propose reverting `rewardMult` 0.35 or restoring map 0's trimmed brutes.
    - Map 2's `PROVISIONAL` comment still stands; Level 3 had not been played when this decision was made. #15/#16 remain open.
9. `[review]` Phase 10 (future): iOS Prep — Capacitor, touch controls, App Store pipeline _(large platform port; App Store submission itself is a [human] step when you reach it)_
10. `[review]` ✅ **Storyline Phase 2 — portrait art** DONE (cinematics still optional/unscheduled) — _code half DONE 2026-08-17 (PR #47): `BootScene` now derives portrait keys from `STORY_SPEAKERS`, probes each PNG 404-tolerantly and calls `registerPortraits()`, so portrait art is a **pure asset drop with zero code change** (light-up proven live: dropping one PNG switched Sol Command to real art while Commander Rael kept its initial block). **Art half DONE 2026-08-20 (PR #52):** all 3 portraits generated (FLUX-schnell, 256×256), on-model and matching each speaker's tonal anchor (Rael gold, Sol Command blue, the Vorn violet); live-verified rendering in the campaign-intro dialog. **Only the optional cinematic treatment for `campaign_intro`/`campaign_ending` remains — not scheduled.**_ — the campaign narrative engine shipped in PR #40 with text/placeholder portraits. Generate the 3 speaker portraits (prompts in `assets/portraits/PROMPTS.md`) and register their keys so `portraitFallback` resolves to real art (zero logic change); optionally add a cinematic/animated treatment for the `campaign_intro` opener and `campaign_ending` finale. _(added 2026-06-21)_ _(mostly an asset drop + optional polish)_

15. `[review]` ✅ **Armour legibility — DUPLICATE of #17, both closed together 2026-09-21.** This item and #17 are the same work, written down twice under different numbers; #17 carries the outcome. _(added 2026-09-20)_. NOT a rebalance; the formula stays. Flat armour floors 8 tower/tier/enemy combos to exactly 1 damage a shot (ice T1 vs brute/colossus/titan, ice T2 vs colossus/titan, ice T3 vs titan, archer T1 vs colossus/titan) — 245 to 1632 shots to kill ONE wave-8 enemy. PR #66 made those hits *visible*; nothing makes them *legible*. The enemy inspector shows `Armor N`, but the tower panel shows only the weakness matrix, so a player cannot tell "my damage is being absorbed" from "this tower is broken". Surface the interaction where the player chooses a tower. Decision on file: **make it legible, do not rebalance.**

20. `[review]` **Tier-4 branch cards don't show the armour interaction** _(raised 2026-09-21 by #17's final review)_. `.branch-card .branch-matchup` shows only the multiplicative matchup, which is the exact gap #17 closed one UI surface over — and the branch pick is **the one purchase decision the armour band would actually change**. Concretely: Ice **T4A Permafrost (28 dmg)** is `heavy` against both colossus and titan, while **T4B Shatter (50 dmg)** is clean against everything; a player choosing between them today is shown nothing that distinguishes them on armour. `describeTowerArmour({ damage, pierce })` already exists and takes exactly what a branch card has, so this is a render change, not new logic. Small.

21. `[auto]` **Tier-4 damage is exercised nowhere in the armour test sweep** _(raised 2026-09-21)_. The derived band sweep in `src/systems/armourLegibility.test.js` walks tiers 1-3 plus `soldierStats`, never `tier4A`/`tier4B`. Ice T4A Permafrost is the only tier-4 combination the feature will actually render and it has no coverage. The sweep's assertions are universally quantified, so adding tier-4 rows cannot make a correct implementation fail — this is coverage, not a latent bug. Two lines. Bundle with #20.

22. `[review]` **Armour legibility for heroes, soldiers and the codex** _(raised 2026-09-21)_. #17 deliberately scoped to the tower panel and the in-world number. The hero inspector, the soldier display and the codex all still show a bare `Armor N` (or nothing) with no indication of what it does to a given attacker. The pure half (`armourAbsorption` / `describeTowerArmour`) is already built and reusable. Decide first whether the hero inspector should show the hero's OWN absorption against the enemy it is fighting, or the enemy's armour against everything — they are different questions and the tower panel answers only the first.

23. `[auto]` **Absorbed damage numbers have no pool priority** _(raised 2026-09-21 by #17's final review; ceiling case, not currently reachable in normal play)_. `DamageNumberOverlay` has a 24-slot pool. The per-enemy 700ms throttle bounds the per-enemy rate but nothing bounds the global rate. Map 9 wave 18 ships 6 titans + 12 brutes; archer T1/T2 are `heavy` against both and fire ~1/s, each number holding a slot for its 1200ms tween — roughly 21+ slots held by `🛡` numbers alone. Past 24, **every** hit is dropped, including the crit/AoE/big numbers that predate this feature and previously had the pool to themselves. Absorbed numbers are the least valuable of the set and should yield first. Needs a priority rule, not a bigger pool.

24. `[auto]` **`Hero.js` and `Soldier.js` HP bars still size from `def.radius`** _(pre-existing; the same defect PR #68 fixed for enemies, deliberately left out of that PR's scope)_. Sprites render 2-6x their `def.radius`, so the bar is drawn inside the body it describes — the exact bug that made players misread archer damage as "not registering". `src/systems/hpBar.js` `hpBarGeometry()` already exists and already handles the sprite-vs-fallback case; this is wiring, not new logic.

25. `[auto]` **Small cleanups found while working in the damage/number path** _(raised 2026-09-21)_. (a) The `Veth ` prefix strip `.replace(/^Veth\s+/, '')` is now duplicated in **six** places (`GameScene.js` x2, `UIScene.js` x2, `InspectController.js`, and the new armour line) — hoist it into `entityDescriptors.js` beside the other shared naming. (b) `DamageNumberOverlay`'s `isCrit` branch is **dead code**: grep finds no producer of `isCrit` anywhere in `src/`. Either wire crits up or delete the branch and its style; leaving an unreachable presentation implies a feature that does not exist. (c) `armourAbsorption` returns `after: 0` for `amount: 0` where `applyArmour(0, armor)` returns `1` — harmless today (no consumer reads `after` on that path) but the two disagree inside the one module pair whose entire purpose is preventing a second copy of the arithmetic from drifting.

26. `[review]` **No touch path to the wave preview** _(known gap recorded inside #19, promoted to its own item 2026-09-21)_. On a touchscreen the Send Wave button is the only thing to tap and tapping it sends the wave. Tap-toggle was tried and removed because it opened the popover *and* fired the wave, leaving a stale preview over a running wave. Hover and keyboard work fully. Options: tap-to-preview-then-tap-to-send, or a separate ⓘ affordance. Matters only if touch is a target — **fold this into the iOS port (#9) rather than doing it standalone.**

27. `[review]` **`rankSlots` is still matchup-blind and range-blind** _(raised 2026-09-22 by #15)_. `src/sim/buildPolicy.js` ranks slots by path coverage at a **hardcoded reference range of 120**, while the sniper reaches 200 and ice 115. #15 fixed WHICH tower the model buys but not WHERE it puts it, so the model can now correctly buy a sniper and then drop it in a slot scored for a different weapon. Deliberately out of #15's scope: slot choice and tower choice become mutually dependent (the best slot depends on which tower goes in it), which is a materially harder problem than ranking a fixed list. Worth doing before trusting per-map numbers to fine precision.

28. `[review]` **The upgrade pass cannot price blocking depth** _(raised 2026-09-22 by #15's final review)_. `TOWER_DEFS.barracks` tiers carry only `soldierStats`, no `damage`/`fireRate`, so `towerSpecAt('barracks', n)` yields 0 DPS at every tier and the scored upgrade pass values every barracks upgrade at exactly **0** — losing to any positive-scoring upgrade forever. Currently **unreachable** on every shipped map (archer T2 at 50g always undercuts barracks T2 at 65g, so cheapest-first never reached it either, and the barracks stays L1 in both A/B columns), so #15 documented it rather than inventing a heuristic. But the `Blocking sensitivity` section exists to price blocking and now runs through a policy that can only buy the OPENING barracks. Fixing it needs an explicit rule in the spirit of `barracksTarget`, not a damage metric — which is a design decision deserving its own brainstorm.

29. `[auto]` **Split the Matchup sensitivity A/B into its two causes** _(raised 2026-09-22)_. `matchupAware` flips both the purchase ranking and the upgrade ordering, and the report says so honestly — but "how much of map 9's −0.76x was tower choice versus upgrade concentration" is not answerable from the table. Two more columns (or a third policy flag) would separate them. Only worth doing if #16 finds the distinction load-bearing; the A/B column is scheduled to be removed after one release anyway.

30. `[human]` **Playtest the campaign end to end** _(raised 2026-09-22 by #16; the notes have wanted this since 2026-09-20)_. Every difficulty number in this project is **simulated**, against a model that is pessimistic by construction — it omits hero abilities, meta upgrades, soldier repositioning and the send-wave-early bonus. #16's values are a calibrated starting point, not a settled answer. **Watch the MID campaign for flatness, not only the late campaign for difficulty** — see #31 for why that is now the likelier failure.

31. `[review]` **The flat +120 logistics upgrade tilts the campaign the wrong way** _(raised 2026-09-22 by #16's final review)_. `log_supply_cache` (2 stars) + `log_deep_reserves` (3 stars) grant a **flat +120 startGold** (`src/systems/UpgradeManager.js:96-97`) — 5 stars, affordable after 3-starring two of maps 0-2, i.e. reachable exactly when a player arrives at map 3. Because it is flat it is worth **+67% on map 3 and only +40% on map 9**, so it flattens the very curve #16 just made monotonic. Measured with those two nodes only, maps 3/4/5 keep **89%/89%/100%** of lives against the report's 90% TOO EASY line — map 5 loses zero of its fifteen lives. Options: scale the bonus with board size, move it later in the star economy, or accept it and tighten maps 3-5. Needs a decision, then a re-run of #16's sweep.

32. `[review]` **Map 9 is still under-funded, and its opening barracks is provably dead gold** _(raised 2026-09-22 by #16's final review)_. #16 took map 9 from 2.89x to 1.36x but it still builds only **3 of 20 towers** and survives 1/18 waves. Survival is steeply gold-elastic right at the shipped value — 600g start takes it to **12/18 waves and 20/20 towers** — so this is residual under-funding, not a wave-shape wall. Compounding it: `DEFAULT_BARRACKS_TARGET = 1` spends the first 100g of the opening hand on a barracks *before* any firing tower, and **map 9's wave 1 is 12 phantoms, which FLY** — tier-1 soldiers cannot block flyers, so that 100g is provably dead on the wave that ends the run. Forcing `barracksTarget: 0` moves map 9 to 3/18 waves and 5 towers. The same artefact makes **map 6's headline 1.05x misleading**: at `barracksTarget: 0` map 6 is **won 15/15 with 3 lives left at 1x damage**. Fixing this is a build-policy change (make the opening barracks conditional on the early waves containing blockable ground enemies), which belongs with #27/#28.

## Completed
<!-- Tags below are retrospective/illustrative — runner skips done items regardless. -->
- `[review]` ~~**Hero XP progress bar** (PR #69). Gold bar under the hero HP bar filling as the hero deals damage, hover tooltip (`Level 2 — 38% to Level 3 · damage dealt 456 / 1,200`), `· MAX` at the cap. New pure `heroXpProgress()` beside the existing levelling maths; `Hero.xpProgress()` accessor; one extra field on the per-frame `hero:update` event. Display-only — `src/data/`/`src/sim/` untouched. Live: tracks truth within **0.047pp across 494 samples**, resets on level-up, `Rael L5 · MAX` at the cap. Review caught two bugs tests could not: memo state leaking across map relaunches (UIScene is one instance) and a memo key missing `level`. 84 files / 1168 tests~~ (2026-09-20)
- `[auto]` ~~**Enemy HP bar lifted off the sprite** (PR #68). Bar geometry came from `def.radius` while sprites were scaled 2–6x, so EVERY enemy's bar was drawn inside its own body (titan: 48px bar in a 138px chest). Root cause of "takes 3-4 shots for an archer to damage a drone" — the archer did a clean 15 a shot, but the narrow green-on-green bar made the player read the 50%/25% colour flips instead. New pure `hpBarGeometry()` sizes from the rendered sprite~~ (2026-09-20)
- `[auto]` ~~**Fire-rate revert fixed** (PR #67). `Tower.upgrade()` bypassed the mod stack, so upgrading during Overcharge/Power Surge left `_baseFireRate` stale and the tower reverted to its PRE-upgrade rate permanently. Rapid Cannon ran at 37% of what it was bought for. New `setBaseFireRate()`~~ (2026-09-20)
- `[auto]` ~~**Every hit now visibly registers** (PR #66). Damage always applied, but both feedback channels were gated on hit size — 19 of 90 tier-1 combos showed nothing at all, worst case 38 hits per pixel of bar. Added a magnitude-independent hit flash (sprite silhouette tint / Graphics disc) and a one-pixel floor on bar depletion. Reproduced first: 20 archer hits on a wave-5 colossus rendered pixel-identically~~ (2026-09-20)
- `[review]` ~~**Complete art set — 10 overworld nodes + 3 portraits, text-free** (PR #52). Generated locally with FLUX.1 [schnell] via the Draw Things HTTP API. **Caught and fixed a prompt bug:** the style anchor read `cohesive 'Last Light' tower-defense art style` and FLUX rendered the quoted title as poster lettering on 6 of 10 nodes (node 8 misspelled "LAST LIGNT"); CFG 0 makes negative prompts inert, so the only fix was removing the quoted noun. A second-order instance appeared while documenting it — the explanatory note was a `>` blockquote adjacent to the anchor, so the parser merged them and fed "LAST LIGHT" back into the prompt. Both fixed, 2 regression tests added. Every image reviewed by eye; all 23 assets serve 200 in production; browser-verified nodes + portrait rendering. 891 tests~~ (2026-08-20)
- `[auto]` ~~**Art pipeline tooling** (PRs #50, #51). `npm run assets` — inventory derived from the code that requests each file (MAPS/STORY_SPEAKERS/SPRITE_MANIFEST) so it cannot drift; detects missing, malformed (reads PNG IHDR), wrong-size, and art placed outside `public/`. `npm run art` — batch generation parsing the PROMPTS.md prose (so retuning art direction needs no code change), with auto port discovery and `sips` downscaling to target sizes. Fixed a markdown line-wrap bug that corrupted hyphenated compounds ("deep- space") in all 10 prompts~~ (2026-08-19)
- `[auto]` ~~**Balance simulator — model the hero + damage-deficit probe** (PR #49). Hero auto-attack modelled from `Hero.update`; `pathLength`/`pointAtProgress` extracted to `pathGeometry.js` and `Hero.setPathPosition` now uses them. New `findWinMultiplier` converts "unwinnable" into a number. **Finding: difficulty cliff — 1.25x→1.96x across maps 0–6, then 3.63x at map 7 and ~4.3x at 8–9**, corroborated by the independent economy table~~ (2026-08-18)
- `[review]` ~~**Headless balance simulator + model-free economy diagnostics** (PR #48, backlog #7 follow-up). Extracted the damage formula from `Enemy.takeDamage` into shared `src/systems/damage.js` so game and sim cannot drift. Sim reuses the real PathManager/WaveManager/stat tables. **Exact finding: every map's gold ceiling is only 1.32–1.75x the cost of one cheap full board**~~ (2026-08-18)
- `[auto]` ~~**Portrait BootScene loader** (PR #47, backlog #10 code half). Portraits became a pure asset drop — `BootScene` derives keys from `STORY_SPEAKERS`, probes 404-tolerantly, calls `registerPortraits()`. Light-up proven live~~ (2026-08-17)
- `[auto]` ~~**Recovered an orphaned bug fix** (PR #46). `MapSelectScene.shutdown()` closing `UpgradeTreeOverlay` symmetrically — commit 806b098 had been stranded on `feature/hero-mgmt-overlay` since 2026-06-05 and reached no integration line. Found during branch cleanup via `git branch -r --no-merged`; reproduced the listener leak with a test (3 leaked across 3 cycles → 0)~~ (2026-08-17)
- `[review]` ~~**Reconciled `main` with the deploy line + fixed production asset 404s** (PR #45). `main` was 141 commits stale because notes wrongly named an integration branch as the deploy line. **Also found every map backdrop 404ing in production** — Vite copies only `publicDir`, so repo-root `assets/` was served by the dev server and dropped from the build. Fixed with `git mv assets public/assets`, zero source change. 812 tests~~ (2026-08-17)
- `[review]` ~~Overarching storyline + per-level mini-stories (backlog #4, Phase 1, **PR #40 merged** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan (PASS) → 8-task subagent-driven execution (Sonnet implementers) → staff code review + polish → browser-verify. *Last Light* campaign (Sol Vanguard vs. Vorn hive, Commander Rael, turning point at Titan's Reach, homeworld finale). New pure `src/systems/storySequence.js` (navigator) + `src/systems/portraitFallback.js` (deferred-asset portrait resolver); `src/data/story.js` rewritten with `STORY_SPEAKERS`/`STORY_SEQUENCES` (opener + 10 briefings + 9 epilogues + ending) + `briefKey`/`epilogueKey`/`victorySequenceId`/`storyLogEntries` helpers + rewritten mid-wave `STORY_PANELS` (`unlock` removed); `src/ui/StoryDialogOverlay.js` multi-panel dialog (Next/Skip/Escape/backdrop). SaveManager v3→v4 `seenStoryBeats` (once-then-replayable) + 📖 Story Log on MapSelect; opener/briefing/epilogue/ending wired in MapSelectScene + GameScene; `StoryManager.getUnlockPanel` removed. 786 tests (52 new); build clean; browser-verified full flow. Portraits + cinematics deferred to backlog #10~~ (2026-06-21)
- `[review]` ~~Phase 8b remaining SFX (backlog #1, **PR #38 merged** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan (PASS) → 4-task subagent-driven execution (Sonnet implementers) → browser-verify. New pure `src/systems/sfxKeys.js` derives `tower-fire-<type>-<branch>` / `enemy-hit-<type>` keys, falling back to the base key when not registered in `SFX_KEYS` (mirrors PR #37 overworld-art deferred-asset pattern). Wired at GameScene tower-fire (`tower.branch`-aware) + Enemy hit (`def.type`-aware, detune preserved). `SFX_KEYS` left unchanged so all calls fall back to base today (0 decode errors); 16 future assets (10 tower-branch + 6 enemy-hit) documented in new `assets/audio/PROMPTS.md` + ATTRIBUTIONS pointer — drop in with zero code change. 9 helper tests; 750 total; build clean; browser-verified live (branch-A tower resolved `tower-fire-archer-A` once registered; enemy-hit fired in real combat)~~ (2026-06-20)
- `[review]` ~~Calibrate per-level economy (backlog #7, **PR #34 merged** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan → 3-task subagent execution → browser-verify. Per-map `startGold` retuned to a ~2-tower opening hand; new per-map `rewardMult` field in `maps.js` (default `?? 1`) threaded through `GameScene`: kill payouts via new DRY `_killReward(reward)` = `round(reward × killGoldMult × rewardMult)`, wave-clear bonus via new `WAVE_CLEAR_BONUS = 38` const × rewardMult, early-send `_computeEarlyBonus` × rewardMult. Model target: full cheap board (`slots×60`) first affordable ~75% through a level (was wave 3). 711 tests; build clean; browser-verified map 0 (gold 170=130+40 bonus, wave-clear +11=round(38×0.3), early-send preview +10g=floor(0.5×~70×0.3); killGoldMult=1). Calibration values are model-derived starting points needing human playtest-tuning for per-map winnability~~ (2026-06-17)
- `[review]` ~~Show tower range before placing (backlog #6, **PR #33 merged** → `feature/map0-path-fit`): brainstorm → spec → plan → subagent execution + two-stage review → browser-verify. New pure `src/systems/rangePreview.js` (`previewRange` mirrors `Tower.js` round(range×mult)); `GameScene` tracks `_buildCursorX/Y` + `_towerRangeMult`, draws a single ring in `_drawZones` that snaps to nearest free slot (else free-floats), gold+fill when affordable/valid, dim red otherwise. 701 tests; build clean; browser-verified snap/scale/deselect on map 1. Caveat: free-float invalid ring `0x884444` is low-contrast on desert maps~~ (2026-06-16)
- `[auto]` ~~PR consolidation: opened **PR #32** (`feature/map0-path-fit` → integration `feature/phase-3-tower-system`, 62 commits) to land natural-fit paths + dev editor + animated backgrounds + terrain-only rollout + calibrated slots in the integration line; resolves the long-standing propagation gap~~ (2026-06-16)
- `[review]` ~~Terrain-only map rollout — ALL 10 maps (map 0 = **PR #30 merged** into `feature/natural-fit-paths`; maps 1–9 = **PR #31 merged**): regenerated 16:9 terrain-only backdrops (no painted roads); worn-road `PathRenderer` (berm + roadbed + normal-offset ruts via `offsetPolyline`) with 7 themed styles `planet/lunar/station/space-lane/organic/scorched-road`; gameplay routes computed in-browser via a median-tone obstacle-avoiding Dijkstra (works on light terrain, dark void, deck, basalt); themed ringed-emplacement build pads for every theme (PlatformRenderer palette-driven, old disc path removed); difficulty-calibrated path-hugging tower slots (10→20 by map id 10,10,11,12,13,14,15,16,18,20, all within ~90px of path so every tower reaches). 697 tests; build clean; browser-verified maps 0,1,3,4,6,8,9 + slot/pad looks. Regeneration prompts in `assets/backgrounds/PROMPTS.md`~~ (2026-06-16)
- `[review]` ~~Terrain-only/road-path pilot (map 0): worn-road PathRenderer layers (berm+roadbed+normal-offset ruts via new `offsetPolyline`) behind a new `planet-road` style; map 0 switched to it + blockers removed + path re-fit; `maps.test` now imports real `PATH_STYLES`. Root-cause fix for path/art mismatch (AI backdrops have no separable road — verified via in-browser pixel probe). Terrain-only backdrop is a user image-gen step (prompt in spec). 682 tests; build clean; browser-verified road look on map 0. spec/plan 2026-06-12-terrain-only-backdrops-road-path~~ (2026-06-12)
- `[review]` ~~Animated background layer (backlog #6, PR pending): reusable `AmbientBackgroundLayer` (depth-5 Graphics) + pure deterministic `ambientFxFamilies` registry (dust/embers/stars/electrical/bio-pulse, seeded via `SeededRandom`, `MAX_ELEMENTS=90`); `ambientFx:{family,seed}` on all 10 maps; GameScene mount + per-frame update + shutdown; `ambientMotion` registry flag seeded at boot from `SaveManager` honoring `prefers-reduced-motion`; "Ambient motion" toggle in SettingsOverlay. 676 tests; build clean; browser-verified layer render + live toggle on map 0~~ (2026-06-12)
- `[review]` ~~Phase 1: Core game loop (Phaser setup, path, basic enemies, HUD)~~ (2026-05-07)
- `[review]` ~~Phase 2: UIScene, Entity Containers, event-based panel~~ (2026-05-08)
- `[review]` ~~Phase 3: Tower system, tier branching, hero abilities, soldiers~~ (2026-05-10)
- `[auto]` ~~Fix: Missing sniper/barracks defs — all 6 towers now selectable~~ (2026-05-12)
- `[review]` ~~Phase 4 implementation: alien enemy shapes, MAP_WAVES, death particles, 38 tests~~ (2026-05-12)
- `[review]` ~~Barracks/Soldier rebuild: soldiers, blocking, respawn, branch picker, reposition, 33 tests~~ (2026-05-14)
- `[review]` ~~Merge PR #2 (Phase 4 alien enemy system)~~ (2026-05-14)
- `[review]` ~~Merge PR #3 (Barracks/Soldier rebuild)~~ (2026-05-14)
- `[review]` ~~Phase 5 spec + brainstorm: 10-map campaign, ProgressManager API, StoryManager, MapSelectScene~~ (2026-05-17)
- `[review]` ~~Phase 5 implementation: 10 maps, phantom/titan enemies, ProgressManager, StoryManager, MapSelectScene, 116 tests, PR #4~~ (2026-05-17)
- `[review]` ~~Merge PR #4 (Phase 5 Maps & Storyline)~~ (2026-05-18)
- `[review]` ~~Phase 6: Hero Unit (Commander Rael) — 3 abilities, respawn, cooldown UI, 142 tests, PR #6~~ (2026-05-19)
- `[review]` ~~Merge PR #6 (Phase 6 Hero Unit)~~ (2026-05-19)
- `[auto]` ~~Visual QA: phantom rings + titan hexagons confirmed rendering~~ (2026-05-19)
- `[review]` ~~Phase 7 brainstorm + design spec + implementation plan~~ (2026-05-19)
- `[review]` ~~Phase 7 implementation: SaveManager, upgrade catalog, UpgradeManager, modifier threading, meta UI, UpgradeTreeOverlay — 169 tests, PR #7~~ (2026-05-19)
- `[review]` ~~In-level Exit button — abandon a run mid-game, return to Map Select; confirm dialog + scene pause; fixed `#tower-panel`/`#game-msg` shutdown leaks — 172 tests, PR #8~~ (2026-05-20)
- `[review]` ~~Merge PR #7 (Phase 7 Meta & Persistence)~~ (2026-05-20)
- `[review]` ~~Merge PR #8 (in-level Exit button + DOM-leak fixes)~~ (2026-05-21)
- `[review]` ~~Phase 8 plan corrections: Task 11 rewritten to match real call sites (spec assumed Tower.fire/Enemy.die/Hero.die/Hero.useAbility — none exist); Task 1 future-version warn; Task 7 Esc/backdrop close; Task 10 ring graphics; Task 12 boss-music trigger~~ (2026-05-25)
- `[review]` ~~Phase 8 Tasks 1–5 implementation (AudioManager block): SaveManager v3+ warn, AudioManager volume/mute/SFX/music state machine, BootScene registry wiring, 190 tests~~ (2026-05-25)
- `[review]` ~~Phase 8 Tasks 6–12, 14 implementation: SettingsOverlay + gear button, DamageNumberOverlay, ShakeController, ParticleSpawner, full call-site wiring (Enemy/Hero/Projectile/GameScene), GameScene mounts polish systems + combat-music state + boss-theme trigger, howler dep removed, 215 tests~~ (2026-05-26)
- `[review]` ~~Merge PR #10 (Phase 8 Audio & Polish wiring) + PR #11 (Phase 8 SFX assets — 23 Kenney CC0 sounds, 248 KB)~~ (2026-05-26 / 2026-05-27)
- `[review]` ~~Phase 9a — Send-Wave-Early bonus (PR #12): WaveManager.isEarlyEligible, live `(+Xg)` wave-button label, `floor(0.5 × Σ reward)` bonus + toast~~ (2026-05-27)
- `[review]` ~~Phase 9b — Tower/Enemy Weakness Matrix (PR #13): 6×6 base matrix + Tier-4 branch overrides + hero matchup, post-armor multiplier in Enemy.takeDamage, UI surfaces on build hover + TowerPanel + branch picker, 263 tests~~ (2026-05-28)
- `[review]` ~~Phase 9c — Click-to-Inspect Overlay (PR #14): InspectController (272 lines, 55 unit tests), describeEnemyMatchups reverse-matrix helper, enemy/hero peek + pin panels with vulnerable/resists lines, 348 tests~~ (2026-05-29)
- `[auto]` ~~Dead-enemy cleanup (PR #16): projectile Container destruction on death~~ (2026-05-30)
- `[review]` ~~Hero path-restriction infrastructure: pathProgress + setPathPosition, 40px corridor snap-or-reject, Soldier-parity post-loop fallthrough, backward-movement + multi-segment boundary tests~~ (2026-05-30 — on branch, PR pending)
- `[review]` ~~Hero roster brainstorm + design spec + implementation plan (22 tasks, ~1900 lines)~~ (2026-05-30 — on `feature/hero-roster`)
- `[review]` ~~Hero roster Phase 1 (T1–T6): Soldier.heal, source builders heroId-aware + burnSource, Enemy burn DoT + vulnerable status, AreaEffectsManager, SentryTurret + cooldown drift fix~~ (2026-05-30)
- `[review]` ~~Hero roster Phase 2 (T7–T12): HEROES registry with Rael + ability impl tests, Hero.js data-driven refactor + back-compat wrappers, GameScene fireAbility dispatcher + 9 stub handlers, weaknessMatrix reads HEROES.matchups, InspectController reads hero.def — 408 tests, no PR yet~~ (2026-05-30)
- `[review]` ~~Hero roster T13 (Engineer Dax): engRepair/engDeployTurret/engPowerSurge pure ability fns + 5 tests, HEROES.engineer registry entry (hex-hardhat draw, anti-armor matchups), GameScene wires _sentries list + _updateSentries + replaces 3 ability-handler stubs — 419 tests, commit 1abb651~~ (2026-05-31)
- `[review]` ~~Hero roster T14 (Scout Vex): scoutMark/scoutVolley/scoutPhase pure ability fns + 5 tests, HEROES.scout registry entry (hooded-scout draw, anti-air matchups), GameScene wires 3 handler stubs (Mark→vulnerable status, Volley→AoE damage with brief line VFX, Phase Sprint→cloak+speed boost via existing Hero.update countdown) — 430 tests, commit e458106~~ (2026-05-31)
- `[review]` ~~Hero roster T15 (Pyromancer Mira): 4 pure ability fns including pyroBurnOnHit + 4 tests, HEROES.pyro entry with onHit:pyroBurnOnHit (3dps/2s burn per melee hit) + anti-swarm matchups, GameScene wires AreaEffectsManager (Immolate aura + Firefield pool) + Flame Wave cone math + inline VFX — 440 tests, commit 24cb967~~ (2026-05-31)
- `[review]` ~~Hero roster T16 (SaveManager v3): selectedHeroId default 'rael', v1→v2→v3 migration with cmd_*→rael_* upgrade rename, isHeroUnlocked(heroId) based on map stars, 8 new tests — 448 tests, commit 40c03d0~~ (2026-05-31)
- `[review]` ~~Hero roster T17 (Upgrades restructure): 25-node tree (4 per hero × 4 + 4 logistics + 5 arsenal), heroUnlock gate on non-Rael nodes, UpgradeManager.getModifiers(heroId) scopes hero modifiers, canPurchase + getNodeState honor heroUnlock — 451 tests, commit e79e17f~~ (2026-05-31)
- `[review]` ~~Hero roster T18 (MapSelect hero picker): 4 cards with portrait/color/locked state + tooltips, click persists selectedHeroId, heroId passed to scene.start, 6 new tests — 457 tests, commit 17e8e08~~ (2026-05-31)
- `[review]` ~~Hero roster T19 (Dynamic HUD): hero:hud-init event rewrites portrait/level/HP-fill/ability icons + tooltips per hero, _onHeroLevelUp reads cached _heroDef.shortName — commit 93e447f~~ (2026-05-31)
- `[review]` ~~Hero roster T20 (Upgrade overlay restructure): 6 branches (4 hero + logistics + arsenal) with title/subtitle headings, locked-hero state for hero-gated nodes — commit 0711ab4~~ (2026-05-31)
- `[auto]` ~~Hero roster T21 (verification): full test suite green, lint skipped (no eslint.config.js), manual play-through deferred~~ (2026-05-31)
- `[auto]` ~~Hero roster T22 (back-compat cleanup): removed HERO_STATS export, HERO_MULTIPLIERS re-export, heroAirstrikeSource alias, legacy overcharge/airstrike/empPulse timer getters; migrated all consumers; stale-comment fix on retained ability wrappers — commits 7df445a + a3a24d2~~ (2026-05-31)
- `[auto]` ~~Hero roster follow-up: UIScene was never launched (pre-existing since Phase 6) — GameScene now scene.launch('UIScene') after hero/economy ready; UIScene.create bootstraps _onHeroHudInit on first paint so hero swap works end-to-end. Browser-verified Engineer/Scout HUD swap — commit 8cb1d20~~ (2026-05-31)
- `[auto]` ~~Hero roster follow-up #6 fix: tower fire-rate mods now compose via multiplier stack (systems/fireRateMods.js). Each ability registers a named mod; _baseFireRate captured once and never overwritten; fireRate = base × product(mods). Resolves Surge × Overcharge collision that left towers permanently at 2× rate. 6 new tests + browser-verified the exact regression sequence — 463 tests, commit 9672cc7~~ (2026-05-31)
- `[auto]` ~~PR #18 mergeded for hero-roster branch (33 commits, +6745/-345, base feature/phase-3-tower-system)~~ (2026-05-31)
- `[review]` ~~Merge PR #17 (hero path-restriction)~~ (2026-05-31)
- `[auto]` ~~Hero roster merge resolution: free-form `moveTo` replaced by path-restricted `moveToProgress`; speed scales by def.stats.moveSpeed × _moveSpeedMult (preserves Scout's Phase Sprint); _facingX derives from movement direction; obsolete constants/exports already cleaned in T22 dropped — commit 70c12ca~~ (2026-05-31)
- `[review]` ~~Merge PR #18 (hero roster)~~ (2026-05-31)
- `[review]` ~~Phase 8b music curation brainstorm + design spec + implementation plan (12 tasks)~~ (2026-05-31)
- `[review]` ~~Phase 8b — music curation (22 CC0 tracks + 2 SFX replacements): 10 map ambient/combat pairs + boss-mid + boss-final, victory/defeat fanfares, ATTRIBUTIONS.md updated, 0 audio decode errors on page load (was 22), `convert-audio.sh` extended with BOSS_DURATION=75s + MUSIC_BITRATE dropped to 64k, audio bundle ~10MB, automated `scripts/fetch-phase-8b-staging.sh` downloader replaces manual staging~~ (2026-05-31)
- `[review]` ~~Merge PR #19 (Phase 8b music curation)~~ (2026-05-31)
- `[auto]` ~~Merge PR #20 (AreaEffects followsTarget.dead handling — Immolate cleanup on hero death)~~ (2026-06-01)
- `[auto]` ~~Merge PR #22 (hoist heroAbilities.test.js imports to top)~~ (2026-06-01)
- `[review]` ~~Merge PR #23 (menu music on MapSelectScene)~~ (2026-06-02)
- `[review]` ~~In-level Pause + Exit buttons (PR #24 merged): port of original PR #8 Exit button + new Pause toggle with overlay, Space-key Pause shortcut, game-over visual disable, shutdown leak fixes~~ (2026-06-03)
- `[review]` ~~Hero Management overlay implemented on `feature/hero-mgmt-overlay` (not yet PR'd): unified Heroes button on MapSelect, left-rail hero cards + right-pane per-hero upgrade tree, locked-card read-only preview, auto-commits selectedHeroId on unlocked-card click~~ (2026-06-04 / 2026-06-05)
- `[review]` ~~Space-themed backgrounds + restricted tower placement design (spec + plan + review-plan) on `feature/space-themed-backgrounds`: AI-bitmap backdrop pipeline (ChatGPT/DALL-E or Midjourney) + procedural overlay (curved bezier path, 6-type blocker registry, theme-styled platform discs), restricted tower placement via TowerPlacementManager.getNearestSlot, barracks reposition snap, 13-task plan~~ (2026-06-06)
- `[review]` ~~Space-themed backgrounds — Tasks 1-13 implementation: 14 commits, 633/633 tests, PR #26 merged + CI green; PNGs downloaded & named in ~/Downloads, integration deferred to next session per .claude/RESUME-pr26-bitmaps.md~~ (2026-06-06 / 2026-06-07)
- `[auto]` ~~PR #26 merged: PathRenderer `quadraticCurveTo` bug fixed (Phaser Graphics has no such method — Canvas-2D API confusion that the lax Proxy mock silently accepted) + 10 ChatGPT bitmaps committed. Phaser-mock whitelist tightened across PathRenderer / PlatformRenderer / blockerTypes tests so this class of bug can't recur~~ (2026-06-07)
- `[review]` ~~Natural-fit paths + tower slots (backlog #7) + dev map editor (PR #27, merged into `feature/space-themed-backgrounds`): brainstorm → spec (`2026-06-12-natural-fit-paths-slots-design.md`) → 9-task plan → subagent-driven execution. Shared `src/systems/pathGeometry.js` (`samplePath` centripetal Catmull-Rom that passes through every control point + `clampToBounds`); `PathRenderer` and `PathManager` both consume it so the visible curve and enemy movement are now identical (PathManager exposes raw `waypoints` for renderer/blockers + dense `path` for movement; GameScene `_renderStaticLayers` rewired to raw waypoints). Dev-only `MapEditorScene` at `?edit=1&map=N` (drag/insert/delete handles, no-build corridor band, slot-count HUD, `E` exports maps.js-ready arrays to clipboard+console) + pure `mapEditorUtils.js`. All 10 maps re-fitted (one commit each). 21 commits; 647/647 tests; production build clean; browser-verified curve-aligned movement + editor interactions + gameplay playtest~~ (2026-06-12)
