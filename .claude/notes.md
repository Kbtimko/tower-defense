# Project: Last Light (Tower Defense)

## Goal
Build a fully playable tower defense game with 10 maps, 6 tower types with tier branching, distinct alien enemy visuals, and a storyline — deployed at https://tower-defense-black.vercel.app

Phases 1–8 + 9a/9b/9c + dead-enemy cleanup + hero path-restriction + hero roster + Phase 8b music + in-level Pause/Exit buttons + menu music + areaeffects-dead-target-fix + test-imports hoist + Hero Management overlay + Space-Themed Backgrounds (PR #26, including the 10 ChatGPT bitmaps) all merged. Natural-Fit Paths (PR #27) + Animated Background Layer done. **Terrain-only map direction:** map-0 pilot (worn-road renderer + computed crater-avoiding route + sandbag emplacement pads) merged via **PR #30** into `feature/natural-fit-paths`; then rolled out to all 10 maps on **PR #31 (open)** — new 16:9 terrain-only backdrops, 7 themed road styles, themed ringed-emplacement pads, and difficulty-calibrated path-hugging tower slots. **ALL feature PRs are MERGED — zero open PRs (verified 2026-06-24).** Backlog done & merged into `feature/map0-path-fit`: show-range #6 (PR #33), economy #7 (PR #34), hero-L2 verify #2 (PR #35), responsive canvas #3 (PR #36), overworld #5 (PR #37), SFX #1 (PR #38), storyline #4 (PR #40). PR #21 (music) merged → main; PR #32 merged `map0-path-fit`→integration (2026-06-16). **OPEN WORK = a release/propagation chain, NOT a feature:** the 6 features merged after #32 sit in `feature/map0-path-fit` but have NOT propagated onward — `map0-path-fit` is **57 commits ahead of integration** (`feature/phase-3-tower-system`) and **119 ahead of `main`**; integration is **63 ahead of main**. So `main` (hence prod, if Vercel deploys main) is missing show-range/economy/responsive/overworld/SFX/storyline. Next: propagate map0-path-fit → integration → main, THEN start entity art (#8).

## Blockers
- None active

## Known Bugs
- None active. _(Resolved 2026-06-18: "hero not blocking on Level 2" verified as NOT a bug — the hero is a ranged auto-attacker by design; non-blocking is documented as a deliberate out-of-scope decision in the hero-path-restriction spec. See backlog #2.)_

## In Progress
- **RELEASE / PROPAGATION (the only open work).** All feature PRs merged; no open PRs. Six features merged into `feature/map0-path-fit` after PR #32 have not propagated to the integration branch or `main`:
  - `feature/map0-path-fit` is **57 commits ahead** of integration `feature/phase-3-tower-system` (last integration merge: PR #32, `bff6936`, 2026-06-16). Contains merges of PR #33/#34/#35/#36/#37/#38/#40.
  - integration is **63 commits ahead** of `main` (main tip `28771ec` = PR #39 docs + PR #21 music).
  - **`feature/phase-3-tower-system` is the repo DEFAULT branch (`origin/HEAD`) = de-facto main + Vercel deploy line.** The separate `main` branch is a diverged legacy line (26 ahead / 63 behind integration) — NOT the deploy branch; reconciling it is a separate decision, left alone for now.
  - **PR #41 OPEN** `feature/map0-path-fit` → `feature/phase-3-tower-system` (2026-06-24): release/propagation, brings all 6 post-#32 features into the deploy line. Clean merge; 786 tests. Merge this to ship show-range/economy/responsive/overworld/SFX/storyline to prod.
- Done & merged into `feature/map0-path-fit` (all browser-verified at merge time): show-range #6 (PR #33, `src/systems/rangePreview.js`), economy #7 (PR #34, `rewardMult`/`_killReward`/`WAVE_CLEAR_BONUS`), hero-L2 verify #2 (PR #35), responsive canvas #3 (PR #36, `src/systems/viewport.js`, Scale.FIT 1280×720), overworld #5 (PR #37), SFX #1 (PR #38, `src/systems/sfxKeys.js`), storyline #4 (PR #40, `src/systems/storySequence.js`/`portraitFallback.js`/`StoryDialogOverlay.js`, SaveManager v4). 786 tests on map0-path-fit tip.
- Deferred-asset PROMPTS awaiting real files (zero code change to light up): `assets/overworld/PROMPTS.md` (10 node PNGs), `assets/audio/PROMPTS.md` (16 SFX), `assets/portraits/PROMPTS.md` (3 portraits). Economy #34 calibration numbers still need human playtest-tuning for winnability.

## Loose ends
- **`git stash@{0}`** `storyline-prep` (on `feature/phase-8b-remaining-sfx`): pre-existing `SESSION_NOTES.md` edits + untracked root `CLAUDE.md` (the project CLAUDE.md is on disk but NOT git-tracked) — stashed this session so the storyline branch started clean. Restore via `git checkout feature/phase-8b-remaining-sfx && git stash pop`.
- **`git stash@{1}`** `wip-notes` (on `feature/overworld-mapselect`): pre-existing `.claude/sessions.md` + `SESSION_NOTES.md` edits — restore via `git checkout feature/overworld-mapselect && git stash pop`.

## Prioritized Backlog
<!-- Risk tiers: [auto] runner may build→test→PR unattended · [review] needs spec/plan approval · [human] you drive. See ~/projects/CLAUDE.md → Backlog Risk Tiers. Note: this is a static Phaser game (no DB/auth/payments) — items are [review] (features/design) or [auto] (localized fixes/chores). -->
1. `[review]` ✅ **Phase 8b — remaining SFX work** — DONE, **PR #38 open** → `feature/map0-path-fit` (2026-06-20). Code derives branch/enemy-specific SFX keys with graceful fallback to the base sound (mirrors PR #37 overworld-art pattern): new pure `src/systems/sfxKeys.js` (`towerFireSfxKey(type,branch,registeredKeys)`, `enemyHitSfxKey(type,registeredKeys)`) returns the specific key if registered in `SFX_KEYS` else the base; wired at GameScene tower-fire + Enemy hit call sites. `SFX_KEYS` unchanged so every call falls back today (0 audio decode errors); the 16 future assets (10 tower-branch + 6 enemy-hit) are documented in `assets/audio/PROMPTS.md` and drop in later with zero code change. 9 helper tests; 750 total; build clean; browser-verified (live branch-A tower resolved `tower-fire-archer-A` once registered; enemy-hit fired in real combat).
2. `[review]` ✅ **Verify hero on Level 2** — DONE (2026-06-18), **not a bug**. Root cause: the hero is a path-positioned ranged auto-attacker (range ~45), never a hard-blocker; `_checkSoldierBlock` is soldier-only **by explicit design** (hero-path-restriction spec §"Hero does NOT block enemies", flagged out-of-scope). Live-verified on Level 2 with `window.__game` introspection: hero spawns on-path, detects enemies in range, attacks (timer resets), and deals damage (skitter 45→17 hp) as they pass — it just doesn't stop them. Hero attack/move/respawn also covered by passing unit tests. _Optional future feature (not scheduled):_ soldier-style hero blocking — would need `_checkSoldierBlock` to include the hero + enemy-vs-hero melee; its own spec/plan.
3. `[review]` ✅ **Responsive canvas** — DONE, **PR #36 open** (2026-06-19). Switched Phaser to `Scale.FIT` at a fixed 1280×720 design res (`autoCenter: CENTER_BOTH`); world content stays aligned with zero reflow (constant internal coords), letterbox bars on off-aspect windows. New pure `src/systems/viewport.js` `gameToPageCss` converts game→CSS px for the tower panel (`GameScene`) + inspect panels (`InspectController`). `index.html` `#game` flex-centers the canvas (removed the `width/height:100%!important` override). 721 tests; build clean; browser-verified at 16:9 / wide / tall (clean letterbox, no clipping) + tower-panel alignment under FIT.
4. `[review]` ✅ **Overarching storyline + per-level mini-stories** — DONE (Phase 1), **PR #40 open** (2026-06-21). *Last Light* campaign: Sol Vanguard vs. the Vorn hive, Commander Rael, turning point at Titan's Reach, finale at the homeworld. New multi-panel story dialog (`src/ui/StoryDialogOverlay.js` + pure `src/systems/storySequence.js`) plays an opener, per-map pre-battle briefing + post-battle epilogue, boss beats, and an ending; content + helpers in `src/data/story.js`; mid-wave banner retained (rewritten). Once-then-replayable via SaveManager v3→v4 `seenStoryBeats` + a 📖 Story Log on MapSelect. Portraits use deferred-asset fallback (`src/systems/portraitFallback.js`). 786 tests; build clean; browser-verified. **Phase 2 (see #10):** real portrait art + cinematic opener/ending.
5. `[review]` ✅ **Map-progression overworld for MapSelect** — DONE, **PR #37 open** (2026-06-20). Sidebar list replaced with a winding-path node graph: per-map `overworldPos`/`overworldArt` in maps.js, pure `src/systems/overworldState.js` `classifyOverworld` (completed/next/unlocked/locked + isFinal), `MapSelectScene._populateOverworld` renders SVG connectors + node `<img>` art with numbered-circle fallback. 741 tests; build clean; browser-verified (states, fallback, node-select→featured, PLAY launch, locked inert). **Node art (10 `overworld_*.png`) not yet generated — prompts in `assets/overworld/PROMPTS.md`; fallback renders until then.**
6. `[review]` ✅ **Show tower range before placing** — DONE, **PR #33 open** (2026-06-16). Range ring at cursor while a tower type is armed, snaps to nearest slot, gold when affordable. `src/systems/rangePreview.js` + `GameScene._drawZones`.
7. `[review]` ✅ **Calibrate per-level economy** — DONE, **PR #34 open** (2026-06-17). Retuned per-map `startGold` to a ~2-tower opening hand + new per-map `rewardMult` scalar (in `maps.js`) that uniformly scales kill payouts (via new `_killReward` helper), wave-clear bonus (`WAVE_CLEAR_BONUS` const), and early-send bonus in `GameScene`. Full cheap board now first affordable ~75% through a level vs. wave 3 today. **Calibration values are model-derived starting points — needs human playtest-tuning for winnability per map.**
8. `[review]` **Production-quality entity art** — heroes/towers/enemies are currently procedurally-drawn vector shapes (towers = `fillCircle(...,18)` + colored ring; enemies = `fillCircle` in `def.color`; heroes = procedural draws). Zero spritesheets/atlases loaded — only the map backdrops are bitmaps. Replace with real sprite art (idle/attack/death frames where it matters), which requires an asset pipeline + a rendering change from Phaser `Graphics` shapes to `Sprite`/spritesheets in `GameScene._drawTowers/_drawEnemies` + entity classes. Biggest remaining visual lever now that environments are polished. Likely decomposes into sub-projects: (a) sprite-rendering infrastructure, (b) enemies, (c) towers, (d) heroes — each its own spec/plan. _(added 2026-06-17)_ _(large, multi-subproject — needs decomposition + specs)_
9. `[review]` Phase 10 (future): iOS Prep — Capacitor, touch controls, App Store pipeline _(large platform port; App Store submission itself is a [human] step when you reach it)_
10. `[review]` **Storyline Phase 2 — portrait art + cinematics** — the campaign narrative engine shipped in PR #40 with text/placeholder portraits. Generate the 3 speaker portraits (prompts in `assets/portraits/PROMPTS.md`) and register their keys so `portraitFallback` resolves to real art (zero logic change); optionally add a cinematic/animated treatment for the `campaign_intro` opener and `campaign_ending` finale. _(added 2026-06-21)_ _(mostly an asset drop + optional polish)_

## Completed
<!-- Tags below are retrospective/illustrative — runner skips done items regardless. -->
- `[review]` ~~Overarching storyline + per-level mini-stories (backlog #4, Phase 1, **PR #40 open** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan (PASS) → 8-task subagent-driven execution (Sonnet implementers) → staff code review + polish → browser-verify. *Last Light* campaign (Sol Vanguard vs. Vorn hive, Commander Rael, turning point at Titan's Reach, homeworld finale). New pure `src/systems/storySequence.js` (navigator) + `src/systems/portraitFallback.js` (deferred-asset portrait resolver); `src/data/story.js` rewritten with `STORY_SPEAKERS`/`STORY_SEQUENCES` (opener + 10 briefings + 9 epilogues + ending) + `briefKey`/`epilogueKey`/`victorySequenceId`/`storyLogEntries` helpers + rewritten mid-wave `STORY_PANELS` (`unlock` removed); `src/ui/StoryDialogOverlay.js` multi-panel dialog (Next/Skip/Escape/backdrop). SaveManager v3→v4 `seenStoryBeats` (once-then-replayable) + 📖 Story Log on MapSelect; opener/briefing/epilogue/ending wired in MapSelectScene + GameScene; `StoryManager.getUnlockPanel` removed. 786 tests (52 new); build clean; browser-verified full flow. Portraits + cinematics deferred to backlog #10~~ (2026-06-21)
- `[review]` ~~Phase 8b remaining SFX (backlog #1, **PR #38 open** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan (PASS) → 4-task subagent-driven execution (Sonnet implementers) → browser-verify. New pure `src/systems/sfxKeys.js` derives `tower-fire-<type>-<branch>` / `enemy-hit-<type>` keys, falling back to the base key when not registered in `SFX_KEYS` (mirrors PR #37 overworld-art deferred-asset pattern). Wired at GameScene tower-fire (`tower.branch`-aware) + Enemy hit (`def.type`-aware, detune preserved). `SFX_KEYS` left unchanged so all calls fall back to base today (0 decode errors); 16 future assets (10 tower-branch + 6 enemy-hit) documented in new `assets/audio/PROMPTS.md` + ATTRIBUTIONS pointer — drop in with zero code change. 9 helper tests; 750 total; build clean; browser-verified live (branch-A tower resolved `tower-fire-archer-A` once registered; enemy-hit fired in real combat)~~ (2026-06-20)
- `[review]` ~~Calibrate per-level economy (backlog #7, **PR #34 open** → `feature/map0-path-fit`): brainstorm → spec → plan → review-plan → 3-task subagent execution → browser-verify. Per-map `startGold` retuned to a ~2-tower opening hand; new per-map `rewardMult` field in `maps.js` (default `?? 1`) threaded through `GameScene`: kill payouts via new DRY `_killReward(reward)` = `round(reward × killGoldMult × rewardMult)`, wave-clear bonus via new `WAVE_CLEAR_BONUS = 38` const × rewardMult, early-send `_computeEarlyBonus` × rewardMult. Model target: full cheap board (`slots×60`) first affordable ~75% through a level (was wave 3). 711 tests; build clean; browser-verified map 0 (gold 170=130+40 bonus, wave-clear +11=round(38×0.3), early-send preview +10g=floor(0.5×~70×0.3); killGoldMult=1). Calibration values are model-derived starting points needing human playtest-tuning for per-map winnability~~ (2026-06-17)
- `[review]` ~~Show tower range before placing (backlog #6, **PR #33 open** → `feature/map0-path-fit`): brainstorm → spec → plan → subagent execution + two-stage review → browser-verify. New pure `src/systems/rangePreview.js` (`previewRange` mirrors `Tower.js` round(range×mult)); `GameScene` tracks `_buildCursorX/Y` + `_towerRangeMult`, draws a single ring in `_drawZones` that snaps to nearest free slot (else free-floats), gold+fill when affordable/valid, dim red otherwise. 701 tests; build clean; browser-verified snap/scale/deselect on map 1. Caveat: free-float invalid ring `0x884444` is low-contrast on desert maps~~ (2026-06-16)
- `[auto]` ~~PR consolidation: opened **PR #32** (`feature/map0-path-fit` → integration `feature/phase-3-tower-system`, 62 commits) to land natural-fit paths + dev editor + animated backgrounds + terrain-only rollout + calibrated slots in the integration line; resolves the long-standing propagation gap~~ (2026-06-16)
- `[review]` ~~Terrain-only map rollout — ALL 10 maps (map 0 = **PR #30 merged** into `feature/natural-fit-paths`; maps 1–9 = **PR #31 open**): regenerated 16:9 terrain-only backdrops (no painted roads); worn-road `PathRenderer` (berm + roadbed + normal-offset ruts via `offsetPolyline`) with 7 themed styles `planet/lunar/station/space-lane/organic/scorched-road`; gameplay routes computed in-browser via a median-tone obstacle-avoiding Dijkstra (works on light terrain, dark void, deck, basalt); themed ringed-emplacement build pads for every theme (PlatformRenderer palette-driven, old disc path removed); difficulty-calibrated path-hugging tower slots (10→20 by map id 10,10,11,12,13,14,15,16,18,20, all within ~90px of path so every tower reaches). 697 tests; build clean; browser-verified maps 0,1,3,4,6,8,9 + slot/pad looks. Regeneration prompts in `assets/backgrounds/PROMPTS.md`~~ (2026-06-16)
- `[review]` ~~Terrain-only/road-path pilot (map 0): worn-road PathRenderer layers (berm+roadbed+normal-offset ruts via new `offsetPolyline`) behind a new `planet-road` style; map 0 switched to it + blockers removed + path re-fit; `maps.test` now imports real `PATH_STYLES`. Root-cause fix for path/art mismatch (AI backdrops have no separable road — verified via in-browser pixel probe). Terrain-only backdrop is a user image-gen step (prompt in spec). 682 tests; build clean; browser-verified road look on map 0. spec/plan 2026-06-12-terrain-only-backdrops-road-path~~ (2026-06-12)
- `[review]` ~~Animated background layer (backlog #6, PR pending): reusable `AmbientBackgroundLayer` (depth-5 Graphics) + pure deterministic `ambientFxFamilies` registry (dust/embers/stars/electrical/bio-pulse, seeded via `SeededRandom`, `MAX_ELEMENTS=90`); `ambientFx:{family,seed}` on all 10 maps; GameScene mount + per-frame update + shutdown; `ambientMotion` registry flag seeded at boot from `SaveManager` honoring `prefers-reduced-motion`; "Ambient motion" toggle in SettingsOverlay. 676 tests; build clean; browser-verified layer render + live toggle on map 0~~ (2026-06-12)
- `[review]` ~~Phase 1: Core game loop (Phaser setup, path, basic enemies, HUD)~~ (2026-05-07)
- `[review]` ~~Phase 2: UIScene, Entity Containers, event-based panel~~ (2026-05-08)
- `[review]` ~~Phase 3: Tower system, tier branching, hero abilities, soldiers~~ (2026-05-10)
- `[auto]` ~~Fix: Missing sniper/barracks defs — all 6 towers now selectable~~ (2026-05-12)
- `[review]` ~~Phase 4 implementation: alien enemy shapes, MAP_WAVES, death particles, 38 tests~~ (2026-05-12)
- `[review]` ~~Barracks/Soldier rebuild: soldiers, blocking, respawn, branch picker, reposition, 33 tests~~ (2026-05-14)
- `[review]` ~~Merge PR #2 (Phase 4 alien enemy system)~~ (2026-05-14)
- `[review]` ~~Merge PR #3 (Barracks/Soldier rebuild)~~ (2026-05-14)
- `[review]` ~~Phase 5 spec + brainstorm: 10-map campaign, ProgressManager API, StoryManager, MapSelectScene~~ (2026-05-17)
- `[review]` ~~Phase 5 implementation: 10 maps, phantom/titan enemies, ProgressManager, StoryManager, MapSelectScene, 116 tests, PR #4~~ (2026-05-17)
- `[review]` ~~Merge PR #4 (Phase 5 Maps & Storyline)~~ (2026-05-18)
- `[review]` ~~Phase 6: Hero Unit (Commander Rael) — 3 abilities, respawn, cooldown UI, 142 tests, PR #6~~ (2026-05-19)
- `[review]` ~~Merge PR #6 (Phase 6 Hero Unit)~~ (2026-05-19)
- `[auto]` ~~Visual QA: phantom rings + titan hexagons confirmed rendering~~ (2026-05-19)
- `[review]` ~~Phase 7 brainstorm + design spec + implementation plan~~ (2026-05-19)
- `[review]` ~~Phase 7 implementation: SaveManager, upgrade catalog, UpgradeManager, modifier threading, meta UI, UpgradeTreeOverlay — 169 tests, PR #7~~ (2026-05-19)
- `[review]` ~~In-level Exit button — abandon a run mid-game, return to Map Select; confirm dialog + scene pause; fixed `#tower-panel`/`#game-msg` shutdown leaks — 172 tests, PR #8~~ (2026-05-20)
- `[review]` ~~Merge PR #7 (Phase 7 Meta & Persistence)~~ (2026-05-20)
- `[review]` ~~Merge PR #8 (in-level Exit button + DOM-leak fixes)~~ (2026-05-21)
- `[review]` ~~Phase 8 plan corrections: Task 11 rewritten to match real call sites (spec assumed Tower.fire/Enemy.die/Hero.die/Hero.useAbility — none exist); Task 1 future-version warn; Task 7 Esc/backdrop close; Task 10 ring graphics; Task 12 boss-music trigger~~ (2026-05-25)
- `[review]` ~~Phase 8 Tasks 1–5 implementation (AudioManager block): SaveManager v3+ warn, AudioManager volume/mute/SFX/music state machine, BootScene registry wiring, 190 tests~~ (2026-05-25)
- `[review]` ~~Phase 8 Tasks 6–12, 14 implementation: SettingsOverlay + gear button, DamageNumberOverlay, ShakeController, ParticleSpawner, full call-site wiring (Enemy/Hero/Projectile/GameScene), GameScene mounts polish systems + combat-music state + boss-theme trigger, howler dep removed, 215 tests~~ (2026-05-26)
- `[review]` ~~Merge PR #10 (Phase 8 Audio & Polish wiring) + PR #11 (Phase 8 SFX assets — 23 Kenney CC0 sounds, 248 KB)~~ (2026-05-26 / 2026-05-27)
- `[review]` ~~Phase 9a — Send-Wave-Early bonus (PR #12): WaveManager.isEarlyEligible, live `(+Xg)` wave-button label, `floor(0.5 × Σ reward)` bonus + toast~~ (2026-05-27)
- `[review]` ~~Phase 9b — Tower/Enemy Weakness Matrix (PR #13): 6×6 base matrix + Tier-4 branch overrides + hero matchup, post-armor multiplier in Enemy.takeDamage, UI surfaces on build hover + TowerPanel + branch picker, 263 tests~~ (2026-05-28)
- `[review]` ~~Phase 9c — Click-to-Inspect Overlay (PR #14): InspectController (272 lines, 55 unit tests), describeEnemyMatchups reverse-matrix helper, enemy/hero peek + pin panels with vulnerable/resists lines, 348 tests~~ (2026-05-29)
- `[auto]` ~~Dead-enemy cleanup (PR #16): projectile Container destruction on death~~ (2026-05-30)
- `[review]` ~~Hero path-restriction infrastructure: pathProgress + setPathPosition, 40px corridor snap-or-reject, Soldier-parity post-loop fallthrough, backward-movement + multi-segment boundary tests~~ (2026-05-30 — on branch, PR pending)
- `[review]` ~~Hero roster brainstorm + design spec + implementation plan (22 tasks, ~1900 lines)~~ (2026-05-30 — on `feature/hero-roster`)
- `[review]` ~~Hero roster Phase 1 (T1–T6): Soldier.heal, source builders heroId-aware + burnSource, Enemy burn DoT + vulnerable status, AreaEffectsManager, SentryTurret + cooldown drift fix~~ (2026-05-30)
- `[review]` ~~Hero roster Phase 2 (T7–T12): HEROES registry with Rael + ability impl tests, Hero.js data-driven refactor + back-compat wrappers, GameScene fireAbility dispatcher + 9 stub handlers, weaknessMatrix reads HEROES.matchups, InspectController reads hero.def — 408 tests, no PR yet~~ (2026-05-30)
- `[review]` ~~Hero roster T13 (Engineer Dax): engRepair/engDeployTurret/engPowerSurge pure ability fns + 5 tests, HEROES.engineer registry entry (hex-hardhat draw, anti-armor matchups), GameScene wires _sentries list + _updateSentries + replaces 3 ability-handler stubs — 419 tests, commit 1abb651~~ (2026-05-31)
- `[review]` ~~Hero roster T14 (Scout Vex): scoutMark/scoutVolley/scoutPhase pure ability fns + 5 tests, HEROES.scout registry entry (hooded-scout draw, anti-air matchups), GameScene wires 3 handler stubs (Mark→vulnerable status, Volley→AoE damage with brief line VFX, Phase Sprint→cloak+speed boost via existing Hero.update countdown) — 430 tests, commit e458106~~ (2026-05-31)
- `[review]` ~~Hero roster T15 (Pyromancer Mira): 4 pure ability fns including pyroBurnOnHit + 4 tests, HEROES.pyro entry with onHit:pyroBurnOnHit (3dps/2s burn per melee hit) + anti-swarm matchups, GameScene wires AreaEffectsManager (Immolate aura + Firefield pool) + Flame Wave cone math + inline VFX — 440 tests, commit 24cb967~~ (2026-05-31)
- `[review]` ~~Hero roster T16 (SaveManager v3): selectedHeroId default 'rael', v1→v2→v3 migration with cmd_*→rael_* upgrade rename, isHeroUnlocked(heroId) based on map stars, 8 new tests — 448 tests, commit 40c03d0~~ (2026-05-31)
- `[review]` ~~Hero roster T17 (Upgrades restructure): 25-node tree (4 per hero × 4 + 4 logistics + 5 arsenal), heroUnlock gate on non-Rael nodes, UpgradeManager.getModifiers(heroId) scopes hero modifiers, canPurchase + getNodeState honor heroUnlock — 451 tests, commit e79e17f~~ (2026-05-31)
- `[review]` ~~Hero roster T18 (MapSelect hero picker): 4 cards with portrait/color/locked state + tooltips, click persists selectedHeroId, heroId passed to scene.start, 6 new tests — 457 tests, commit 17e8e08~~ (2026-05-31)
- `[review]` ~~Hero roster T19 (Dynamic HUD): hero:hud-init event rewrites portrait/level/HP-fill/ability icons + tooltips per hero, _onHeroLevelUp reads cached _heroDef.shortName — commit 93e447f~~ (2026-05-31)
- `[review]` ~~Hero roster T20 (Upgrade overlay restructure): 6 branches (4 hero + logistics + arsenal) with title/subtitle headings, locked-hero state for hero-gated nodes — commit 0711ab4~~ (2026-05-31)
- `[auto]` ~~Hero roster T21 (verification): full test suite green, lint skipped (no eslint.config.js), manual play-through deferred~~ (2026-05-31)
- `[auto]` ~~Hero roster T22 (back-compat cleanup): removed HERO_STATS export, HERO_MULTIPLIERS re-export, heroAirstrikeSource alias, legacy overcharge/airstrike/empPulse timer getters; migrated all consumers; stale-comment fix on retained ability wrappers — commits 7df445a + a3a24d2~~ (2026-05-31)
- `[auto]` ~~Hero roster follow-up: UIScene was never launched (pre-existing since Phase 6) — GameScene now scene.launch('UIScene') after hero/economy ready; UIScene.create bootstraps _onHeroHudInit on first paint so hero swap works end-to-end. Browser-verified Engineer/Scout HUD swap — commit 8cb1d20~~ (2026-05-31)
- `[auto]` ~~Hero roster follow-up #6 fix: tower fire-rate mods now compose via multiplier stack (systems/fireRateMods.js). Each ability registers a named mod; _baseFireRate captured once and never overwritten; fireRate = base × product(mods). Resolves Surge × Overcharge collision that left towers permanently at 2× rate. 6 new tests + browser-verified the exact regression sequence — 463 tests, commit 9672cc7~~ (2026-05-31)
- `[auto]` ~~PR #18 opened for hero-roster branch (33 commits, +6745/-345, base feature/phase-3-tower-system)~~ (2026-05-31)
- `[review]` ~~Merge PR #17 (hero path-restriction)~~ (2026-05-31)
- `[auto]` ~~Hero roster merge resolution: free-form `moveTo` replaced by path-restricted `moveToProgress`; speed scales by def.stats.moveSpeed × _moveSpeedMult (preserves Scout's Phase Sprint); _facingX derives from movement direction; obsolete constants/exports already cleaned in T22 dropped — commit 70c12ca~~ (2026-05-31)
- `[review]` ~~Merge PR #18 (hero roster)~~ (2026-05-31)
- `[review]` ~~Phase 8b music curation brainstorm + design spec + implementation plan (12 tasks)~~ (2026-05-31)
- `[review]` ~~Phase 8b — music curation (22 CC0 tracks + 2 SFX replacements): 10 map ambient/combat pairs + boss-mid + boss-final, victory/defeat fanfares, ATTRIBUTIONS.md updated, 0 audio decode errors on page load (was 22), `convert-audio.sh` extended with BOSS_DURATION=75s + MUSIC_BITRATE dropped to 64k, audio bundle ~10MB, automated `scripts/fetch-phase-8b-staging.sh` downloader replaces manual staging~~ (2026-05-31)
- `[review]` ~~Merge PR #19 (Phase 8b music curation)~~ (2026-05-31)
- `[auto]` ~~Merge PR #20 (AreaEffects followsTarget.dead handling — Immolate cleanup on hero death)~~ (2026-06-01)
- `[auto]` ~~Merge PR #22 (hoist heroAbilities.test.js imports to top)~~ (2026-06-01)
- `[review]` ~~Merge PR #23 (menu music on MapSelectScene)~~ (2026-06-02)
- `[review]` ~~In-level Pause + Exit buttons (PR #24 merged): port of original PR #8 Exit button + new Pause toggle with overlay, Space-key Pause shortcut, game-over visual disable, shutdown leak fixes~~ (2026-06-03)
- `[review]` ~~Hero Management overlay implemented on `feature/hero-mgmt-overlay` (not yet PR'd): unified Heroes button on MapSelect, left-rail hero cards + right-pane per-hero upgrade tree, locked-card read-only preview, auto-commits selectedHeroId on unlocked-card click~~ (2026-06-04 / 2026-06-05)
- `[review]` ~~Space-themed backgrounds + restricted tower placement design (spec + plan + review-plan) on `feature/space-themed-backgrounds`: AI-bitmap backdrop pipeline (ChatGPT/DALL-E or Midjourney) + procedural overlay (curved bezier path, 6-type blocker registry, theme-styled platform discs), restricted tower placement via TowerPlacementManager.getNearestSlot, barracks reposition snap, 13-task plan~~ (2026-06-06)
- `[review]` ~~Space-themed backgrounds — Tasks 1-13 implementation: 14 commits, 633/633 tests, PR #26 open + CI green; PNGs downloaded & named in ~/Downloads, integration deferred to next session per .claude/RESUME-pr26-bitmaps.md~~ (2026-06-06 / 2026-06-07)
- `[auto]` ~~PR #26 merged: PathRenderer `quadraticCurveTo` bug fixed (Phaser Graphics has no such method — Canvas-2D API confusion that the lax Proxy mock silently accepted) + 10 ChatGPT bitmaps committed. Phaser-mock whitelist tightened across PathRenderer / PlatformRenderer / blockerTypes tests so this class of bug can't recur~~ (2026-06-07)
- `[review]` ~~Natural-fit paths + tower slots (backlog #7) + dev map editor (PR #27, merged into `feature/space-themed-backgrounds`): brainstorm → spec (`2026-06-12-natural-fit-paths-slots-design.md`) → 9-task plan → subagent-driven execution. Shared `src/systems/pathGeometry.js` (`samplePath` centripetal Catmull-Rom that passes through every control point + `clampToBounds`); `PathRenderer` and `PathManager` both consume it so the visible curve and enemy movement are now identical (PathManager exposes raw `waypoints` for renderer/blockers + dense `path` for movement; GameScene `_renderStaticLayers` rewired to raw waypoints). Dev-only `MapEditorScene` at `?edit=1&map=N` (drag/insert/delete handles, no-build corridor band, slot-count HUD, `E` exports maps.js-ready arrays to clipboard+console) + pure `mapEditorUtils.js`. All 10 maps re-fitted (one commit each). 21 commits; 647/647 tests; production build clean; browser-verified curve-aligned movement + editor interactions + gameplay playtest~~ (2026-06-12)
