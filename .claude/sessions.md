# Session Log: Last Light (Tower Defense)

---
## 2026-09-22 — Session Summary
**Accomplished:** Three backlog items shipped end to end, all merged. **PR #76 (backlog #17)** — armour-floor legibility: a `🛡 Armour absorbs:` percentage line on the tower panel plus muted, shield-marked in-world numbers for hits the sub-30 gate used to hide. **PR #77 (backlog #15)** — matchup-aware `greedyBuildPlan`: both the purchase and upgrade passes now score through the real `computeDamage` against the enemies still to come, replacing a metric that ranked the archer first while it lands 1.0 DPS on a titan. **PR #78 (backlog #16)** — campaign economy ramp: maps 3/4/5 went UNWINNABLE → OK and maps 6-9 from 1.88/2.23/2.80/2.89x → 1.05/1.30/1.36/1.36x. Production verified after each merge; the live bundle hash matches a local build of merged `main`.

**Decisions made:** Armour absorption is measured on the armour step ALONE and honours `pierce` (never `final / raw`, which conflates it with the weakness matrix). The in-world absorbed marker is gated to `source.kind === 'tower'` so damage-over-time cannot spam it. `towerValue` keeps its name rather than being renamed, and its naive comparator stays byte-identical so the fallback every existing sim test runs through cannot shift. Economy values are DERIVED from two constants against a fully-upgraded board — the cheapest-board metric cannot see tier 4 and hid the defect. Maps 0-2 stay frozen per #18 and are now pinned by a guard. Map 5 carries a recorded per-map override because the board-based rule over-funded a map with more slots but less threat. The map-2 startGold non-monotonicity was REPORTED, not tuned away.

**Where we left off:** Nothing in progress, no open PRs, production healthy. Backlog's top items are **#30** (human playtest — every difficulty number in this project is simulated against a deliberately pessimistic model, and #78 just shipped a balance change on that basis), **#31** (the flat +120 logistics upgrade arrives exactly at map 3 and is worth +67% there vs +40% on map 9, so it may have flattened the mid campaign — maps 3/4/5 reach 89/89/100% of lives kept with just two cheap nodes), and **#32** (map 9 still builds 3/20 towers; its forced opening barracks is dead gold against a wave-1 of twelve FLYING phantoms). Loose ends: 9 worktrees and ~20 local branches all on merged branches want a sweep, and the primary checkout sits on merged `feat/hero-melee-and-regen` with uncommitted session bookkeeping — both left alone deliberately.

---
## 2026-09-20 — Session Summary
**Accomplished:** Four merged PRs, all from one report of "damage doesn't register with enemies". **#66** — damage was always applying (`computeDamage` floors at 1), but both feedback channels were gated on hit SIZE: 19 of 90 tier-1 (tower, enemy, wave) combos showed literally nothing, worst case 38 hits per pixel of HP bar. Reproduced it first — 20 archer hits on a wave-5 colossus rendered pixel-identically — then added a magnitude-independent hit flash and a one-pixel floor on bar depletion. The user said it was still broken, which was correct: **#67** found `Tower.upgrade()` bypassing the fire-rate mod stack, so upgrading during Overcharge left `_baseFireRate` stale and reverted the tower permanently (Rapid Cannon at 37% of what it was bought for). Still not it: **#68** found the real cause of "takes 3-4 shots for an archer to damage a drone" — every enemy's HP bar was drawn ON its own sprite, because bar geometry came from `def.radius` while sprites were scaled up 2–6x (titan: a 48px bar inside a 138px chest). The archer was doing a clean 15 damage every shot; the narrow green-on-green bar just made the player read the 50%/25% colour flips, which on a 70 HP drone land on hits 3 and 4. Then **#69**, the session's one feature: a hero XP progress bar, run through the full pipeline (brainstorm → spec → plan → 7 subagent tasks with two-stage review each → live verification → PR). Suite **1103 → 1168 tests** (81 → 84 files).

**Decisions made:** Armour stays as-is — flat subtraction floors 8 tower/enemy combos to 1 damage a shot (5m50s to 38m51s to kill one wave-8 enemy), and the call was **make it legible, do not rebalance** (now backlog #15, not started). The XP bar is display-only: `src/data/` and `src/sim/` untouched, and `heroXpProgress()` delegates to the existing `heroLevelForDamage()` so the headless simulator cannot drift. Bar placed under the HP bar in the HUD strip with detail on hover, rather than a portrait ring or a floating on-map bar. Test threshold values must be DERIVED, never written as literals (`0.07 * 8800` is `616.0000000000001`, not `616`).

**Where we left off:** `main` is at `a3c717d` with everything merged and live; no open PRs; 84 files / 1168 tests; build clean. Next most valuable thing is still a **human playtest of the full campaign** — it has only ever been simulated. Backlog #15 (armour legibility) is specified and unstarted. `Hero.js`/`Soldier.js` HP bars still derive geometry from `def.radius` and carry the same latent defect fixed for enemies in #68. A worktree remains at `~/.config/superpowers/worktrees/tower-defense/fix-enemy-hit-feedback`. **Note for next session: another Claude session was working in the primary checkout concurrently and hard-reset it mid-task, destroying uncommitted work — use a worktree, and back up a patch before any branch operation there.**

---
## 2026-09-20 — Session Summary
**Accomplished:** Settled the one question blocking PR #58 and shipped it. Measured the titan→colossus swap across three full balance states and found the notes' reading of it was wrong in two ways: the swap is uniform (−1 titan / +2 colossus on one wave of each of maps 5–9, only the starting titan count differs), and it is *not* a uniform easing — maps 4, 5, 6 got **harder**, maps 8, 9 did not move in the no-barracks column, and **map 7 was the only map whose no-barracks number moved at all** (3.63x → 3.35x). Held map 7 out of the swap (one line, `src/data/waves.js:122`), restoring 3.63x / 3.08x / 2.80x — identical to baseline in all three columns — at zero cost to the colossus, which still recurs on maps 4, 5, 6, 8, 9. Two regression tests encode the decision. Then resolved PR #58's `.claude/notes.md` conflict after #57 merged, and verified production live.

**Decisions made:**
- **Map 7 is held out of the colossus swap** (option (a)), preserving backlog #12's frozen cliff rather than reversing it. Reverting costs one of five recurrences; accepting would have cost a 17% narrower cliff.
- **Root cause named:** armour is *flat subtraction* (`max(1, dmg - armor)`), so armour 20→15 multiplies tower throughput by `(d-15)/(d-20)` — 2x at 25 damage, up to 5x at or under 20. Against it, `lives--` is per *leaked enemy*, so a swap that leaks costs 2 lives instead of 1. Those two opposed mechanisms are why the swap's direction flips across the campaign.
- **Accepted a known residual:** reverting map 7 restores map 7 exactly but not the *step*, which has two ends — map 6 still carries its own swap (1.96x→2.01x), so the step is +1.62, not +1.67. A slightly harder map 6 sharpens the ramp into the cliff, the direction #12 wanted.

**Where we left off:** **Everything is shipped; nothing is in flight.** PRs #57 (`d9772a9`) and #58 (`27341d1`) are merged and live. Production verified against the canonical host, not just a local build: served bundle hash matches a local build of `main`, **62/62 image assets 200**, 39 sprite sheets serve, and the live bundle's `MAP_WAVES` confirms map 7 ships 17 titans / zero colossus. 983 tests green, build clean.

Only two optional items remain: **#13 overworld node contrast polish** (small, art-only, needs the local Draw Things pipeline) and **#9 the iOS port** (large). Loose ends: seven local / five remote merged branches want a sweep, and the long-standing uncommitted `SESSION_NOTES.md` edit plus untracked `.claude/prompts/` are still in the working tree.

---
## 2026-09-19 — Session Summary
**Accomplished:** Closed backlog **#8 entirely** — all 18 entities now render as real sprite art. **PR #57** (`feat/tower-hero-sprite-art`, 14 commits, 983 tests) ships 27 sheets from 12 generated references: 6 towers (static idle + firing beat; barracks never fires), 4 heroes (idle + gait + attack), soldier and sentry. **PR #58** (`feat/colossus-waves`, 966 tests) gives the colossus a place in the campaign, closing both decisions PR #56 had left open. Also found and fixed four defects the backlog never mentioned: two silent parser failures (a prompt bullet's name must be the runtime entity `type`; a fenced prompt must beat an inline backtick span — three entities were about to be generated from the literal string `assets/sprites/heroes/`), and two wiring gaps (towers never called `setFacing`; the soldier never entered its `attack` state). The sprite compositor became category-aware, proven behaviour-preserving by rebuilding the cached enemies **byte-identical**.

**Decisions made:** Tower facing uses `flipX`, not rotation — the 3/4 base disc tilts badly. Tower tier keeps `_bg` as a ring without its fill disc, because tier is encoded *only* in that stroke width. No `death` art for heroes/soldiers/sentries; the reserved destroy-delay wiring stays untouched. Colossus is **not a boss** — at 400hp/armour 15 it is exactly half a titan and fills a 6.7x gap; it debuts on map 4 (the only map between two introductions that taught nothing new) and recurs on 5-9 via an HP-neutral titan swap. Phantom's sprite scale **stays at 0.58** despite rendering at 1.77x its hitbox where the other five sit at 1.28-1.35x — its bbox is mostly tendril and wisp, and it is the plurality enemy on maps 7-9.

**Where we left off:** Nothing mid-flight. **PRs #57 and #58 are both open, MERGEABLE and unreviewed**; merging either auto-deploys to production, so neither was merged. One open question on #58: the titan swap narrows the map-7 cliff (no-barracks 3.63x → 3.35x) that backlog #12 decided should stay — reverting just the map-7 substitution restores it. Both PRs touch `.claude/notes.md`, so expect a small conflict in whichever merges second (keep #57's header plus #58's backlog item 14). Remaining backlog is only **#13** (overworld node contrast polish on nodes 6 and 7 — a prompt tweak plus regeneration, no code) and **#9** (iOS/Capacitor port). `SESSION_NOTES.md` and `.claude/prompts/` remain uncommitted — pre-existing session bookkeeping, deliberately left alone.

---
## 2026-09-19 — Enemy sprite art: all six Veth enemies (PR #56, open)

**Accomplished:** Closed backlog **#8(b)**. All six enemies (drone, skitter, brute, colossus, phantom, titan) now render as sprites with a looping `move` and a one-shot `death`. **35/35 assets, 968 tests, build clean.** PR #56 open against `main`, 7 commits, 29 files.

Run as a spike first per the brief: drone end-to-end, gated on approval, before the other five. The gate held — the drone shipped, and the approach generalised without rework.

*Pipeline (not the one PROMPTS.md specified):* that document assumed SDXL + IP-Adapter/ControlNet in ComfyUI; none of it is installed (only FLUX.1 schnell), and OpenPose is human-skeleton-trained so it is meaningless for a four-legged chitin drone. Instead: **one generated reference per enemy, every frame derived from it by transform** — consistency by construction rather than constraint. `rembg` (`isnet-general-use`) → largest-connected-component isolation → mirror → bob/rock for move, collapse + eased value-noise dissolve for death. New `scripts/build-sprite-sheets.py`, `npm run art -- --kind sprite`, and `parseSpritePrompts` (all 18 entities parse, so #8 c/d has a path).

*Two code fixes that only became visible once art existed:* the **facing deadzone** (`src/systems/facing.js`) and the **death destroy-delay** in `_fadeOutDeadEnemy`.

**Decisions made:**
- **Derive frames from one reference rather than generate them.** Accepts non-articulating limbs; at 27–66px a leg is 1–2px so a walk cycle is sub-pixel noise, while identity drift reads as flicker. Documented as the explicit trade in PROMPTS.md, with the IP-Adapter route kept as the option if sprites ever render much larger.
- **Death animations included** (maintainer overrode the recommendation to skip). They cost no extra generation — the dissolve derives from the same reference, so it is a transform, not a new image. The initial "doubles the art" estimate was wrong.
- **Facing deadzone at the enemy call site, not in `setFacing`** — Hero passes ±1 units to the same method, so a deadzone there would have stopped heroes turning entirely. Caught before shipping.
- **Cell size scales with the creature** (colossus 96, titan 128) rather than a fixed 64; `scale` set from measured silhouette width, not by eye.

**Corrections worth remembering:** two confident claims were wrong and measurement caught both. (1) The facing flips were called "sub-pixel spline noise" — they are **real 13–26px backtracks**, which falsified the proposed longer-look-ahead fix (measured unchanged at 8/16/24px; only a deadzone works). (2) A first visual check of the death animation looked like a failure and was nearly reported as one; it was **inconclusive** — a corpse mid-dissolve looks like a live drone. Instrumenting destroy latency settled it: 3001/3016/3017/3017/3000 ms for a 6-frame 2fps sheet.

**Where we left off:** **PR #56 is open and unmerged** — merging auto-deploys to production. Two maintainer decisions sit inside it: (1) **`colossus` is spawned by no wave in any map** — its art ships and is correct but is unreachable in normal play, verified only via a temporary local wave patch (reverted); (2) **phantom is the weakest read** — translucent wings washed out at the 0.43 its radius implies, shipped at 0.58, and is the one to re-roll if any. Next up is **#8(c) towers and #8(d) heroes/soldiers/sentries**, now unblocked: the pipeline exists and parses all 18 entities. Note for those: the shared style anchor's "sci-fi tower-defense *unit*" biases the generator toward emplacements — right for towers, wrong for creatures.

---
## 2026-08-17 → 2026-08-20 — Repo reconciliation, production 404 fix, balance simulator, complete art set (PRs #44–#52)

**Accomplished:** Nine PRs merged across a session that began as "revisit the backlog" and turned into unblocking a stale, partly-broken production deploy.

*Repo/deploy reconciliation:* Landed a doc commit stranded on a dead branch (#44). Discovered `main` — not the branch the notes named — is the Vercel production branch, and the live site was **141 commits stale since 2026-06-13**, missing 8 features. Reconciled it (#45), resolving a `MapSelectScene.create()` conflict by keeping both menu music and the story-log/campaign-intro. Pruned 24 remote + 21 local branches; repointed `origin/HEAD`.

*Production bug found during verification:* every map backdrop was **404ing in production** — Vite copies only `publicDir`, so repo-root `assets/` was served by the dev server and silently dropped from the build. Broken since PR #26. Fixed with `git mv assets public/assets`, zero source change.

*Orphaned fix recovered (#46):* branch cleanup surfaced commit 806b098, stranded since 2026-06-05 and in no integration line — `MapSelectScene.shutdown()` leaked an `UpgradeTreeOverlay` listener per scene re-entry. Reproduced with a test (3 leaked → 0).

*Deferred-asset completion (#47):* portraits became a pure asset drop via a `BootScene` 404-tolerant probe.

*Balance work (#48, #49):* extracted the damage formula out of `Enemy.takeDamage` into shared `src/systems/damage.js` so game and simulator cannot drift; built a headless time-stepped simulator reusing the real PathManager/WaveManager/stat tables, plus model-free economy diagnostics. Modelled the hero and added a `findWinMultiplier` probe.

*Art pipeline + assets (#50, #51, #52):* `npm run assets` (inventory derived from the code that requests each file; detects misplaced/malformed/wrong-size) and `npm run art` (batch generation parsing the PROMPTS.md prose). Generated all 10 overworld nodes + 3 portraits locally with FLUX-schnell via the Draw Things HTTP API. **23/23 assets now serve 200 in production.**

**Decisions made:**
- `main` is the production branch; never infer the deploy target from `origin/HEAD`.
- All runtime art lives under `public/assets/` — one location, so the build-omission mistake cannot recur.
- Shared-formula extraction over duplication: `computeDamage`, `pointAtProgress`/`pathLength`, and `portraitPath` are each imported by both the game and its tooling, chosen specifically so a model cannot drift from the thing it models.
- The balance simulator reports only; it never edits `maps.js`. Retuning is the user's call.
- Reported the uncomfortable balance result honestly (all maps unwinnable tower-only) rather than tuning thresholds until it looked healthy — then converted it into an actionable deficit number.
- PROMPTS.md stays the single source of truth for art direction; the generator parses the prose so retuning needs no code change.

**Key finding — difficulty cliff:** the damage multiplier a modelled defence needs ramps smoothly 1.25x→1.96x across maps 0–6, then jumps to **3.63x at map 7 and ~4.3x at maps 8–9**. Corroborated independently by the model-free economy table (gold-per-enemy-HP falls ~4x across the campaign). The last three maps pay less and demand more.

**Where we left off:** Working tree clean, no open PRs, all branches pruned, 891 tests passing, production live and current with all art. Next candidates: **backlog #12** (act on the balance cliff — recommend modelling soldier *blocking* first, the largest unmodelled factor, before retuning any numbers), **#8 b/c/d** (entity sprite art — the genuinely hard one: multi-frame character consistency, needs SDXL + ControlNet, and no SDXL model is downloaded), **#13** (low-priority contrast polish on two dark overworld nodes), **#9** (iOS/Capacitor port).

**Watch out:** generating on this 24 GB machine wedged Draw Things twice via swap exhaustion (12.3 GB model + ~6.5 GB Chrome). Close Chrome and batch small, or use a smaller quantization.

---
## 2026-06-20 — Backlog #1 (Phase 8b Remaining SFX): full pipeline → PR #38

**Accomplished:** Shipped backlog #1 end-to-end through the standard pipeline: session-summary → brainstorm (2 AskUserQuestion decisions) → spec → plan → review-plan (PASS) → 4-task subagent-driven execution (Sonnet implementers) → consolidated review → browser-verify → PR. New pure `src/systems/sfxKeys.js` (`towerFireSfxKey(type, branch, registeredKeys)`, `enemyHitSfxKey(type, registeredKeys)`) derives branch/enemy-specific SFX keys with a graceful fallback to the existing base key, mirroring the PR #37 overworld-art deferred-asset pattern. Wired at the GameScene tower-fire call site (now `tower.branch`-aware) and the Enemy hit call site (`def.type`-aware, random detune preserved). `SFX_KEYS` left unchanged so every call falls back to the base sound today (0 audio decode errors); the 16 future assets (10 tower-branch + 6 enemy-hit) are documented in new `assets/audio/PROMPTS.md` + a pointer in `public/audio/ATTRIBUTIONS.md`. 9 helper unit tests; 750/750 full suite; clean build. Branched `feature/phase-8b-remaining-sfx` off `origin/feature/map0-path-fit`; opened **PR #38** against `feature/map0-path-fit`. Also observed PR #37 (overworld) had merged.

**Decisions made:** (1) Helper takes the registered-keys list (`SFX_KEYS`) as an argument rather than importing it — pure, no module coupling, fully unit-testable. (2) Defer all 16 audio assets — ship code + fallback + curation note now (vs. curating CC0 sounds this session); `SFX_KEYS` only grows when real `.mp3`s land, so no decode-error risk and zero code change when they do. (3) For a ~40-line, fully-spec'd mechanical change, did one consolidated spec+quality review of the complete diff rather than per-task reviewer dispatches — proportionate, and the per-task implementers already ran TDD green.

**Where we left off:** PR #38 open and waiting, stacked on `feature/map0-path-fit` alongside PRs #33/#34/#36. Merge order unchanged: **#32 into the integration line first**, then the children rebase cleanly. Browser-verified the call-site plumbing live (a real Tier-4 branch-A archer tower resolved `tower-fire-archer-A` once that key was registered; enemy-hit fired in real combat) — killed my dev server on :5174 at end. Loose end: pre-existing uncommitted `.claude/sessions.md` + `SESSION_NOTES.md` edits from `feature/overworld-mapselect` are parked in `git stash@{0}` (restore on that branch with `git stash pop`). Next backlog candidates: #4 storyline or #3 responsive canvas (#3 may already be PR #36 — verify).

---
## 2026-05-30 — Hero Roster Brainstorm + Plan + Phase 1+2 Execution

**Accomplished:** Brainstormed and scoped backlog item #1 (Additional heroes with different skills) end to end. Wrote and committed the design spec (`docs/superpowers/specs/2026-05-30-hero-roster-design.md`, 855 lines, commit `9f502bf`) and the 22-task implementation plan (`docs/superpowers/plans/2026-05-30-hero-roster.md`, ~1900 lines, commit `4a17824`). Created branch `feature/hero-roster` off `origin/feature/phase-3-tower-system` (the local `feature/phase-3-tower-system` was stale at PR #6 — `git rebase --onto origin/...` corrected the base). Executed Phase 1 (T1–T6, foundations) and Phase 2 (T7–T12, HEROES registry + Hero data-driven refactor) via `subagent-driven-development`. 19 commits on the branch; 408 tests passing (up from 348). Each task ran implementer (model: sonnet) + spec reviewer + code quality reviewer; reviewers caught 3 real plan/code bugs (T1 test assertion mis-stated hp=0 when Soldier.takeDamage doesn't clamp; T6 sentry cooldown overshoot would have failed the fire-rate test; T10 missing heroId arg to getModifiers) and 1 real implementation bug (T6 sentry cooldown drift caused burst-fire when an enemy appears after an idle period — fixed with clamp + regression test). 2 reviewer over-flags ignored after cross-checking against the plan (T9's moveTo dead-guard and takeDamage signature change were both spec'd in the plan).

**Decisions made:** (1) Branched off `origin/feature/phase-3-tower-system` (production main) rather than `feature/hero-path-restriction` (in-flight, unmerged). Hero roster will merge independently of the path-restriction PR; when path-restriction lands, all heroes inherit it through shared Hero.update movement code. (2) Documented the "spec deviation" up-front in the plan header so subagents knew Hero.moveTo(x,y) is the API on this branch (not moveToProgress which is in the path-restriction PR). (3) Architecture is Approach A (data registry, `src/data/heroes.js` + single Hero class reading `this.def = HEROES[heroId]`) — not subclasses. (4) Back-compat shims (HERO_STATS export, HERO_MULTIPLIERS export, heroAirstrikeSource alias, legacy overcharge/airstrike/empPulse wrappers, _timers getters) kept through T22 cleanup. (5) `_cloakTimer` and `cloaked` are forward-compat only — enemies currently don't target hero, so Scout Phase Sprint's observable effect is the move-speed boost (cloak gates enemy targeting once hero-blocking lands). (6) Stashed the user's prior in-flight edits to .claude/notes.md / sessions.md / SESSION_NOTES.md as `stash@{0}` on `feature/hero-path-restriction` before switching branches — restorable with `git stash pop` when user returns to that branch.

**Where we left off:** Phase 1 (T1–T6) + Phase 2 (T7–T12) complete on `feature/hero-roster`. 408/408 tests passing. Rael runs work identically to production; codebase is ready to receive Engineer/Scout/Pyromancer as 1-config-entry additions. Plan T13–T22 remaining: Phase 3 (T13–T15) adds the three new heroes + GameScene handler wiring + AreaEffectsManager wiring for Pyromancer; Phase 4 (T16–T17) is SaveManager v3 migration + per-hero upgrade tree restructure; Phase 5 (T18–T20) is MapSelectScene hero picker + UIScene dynamic HUD + UpgradeTreeOverlay restructure; Phase 6 (T21–T22) is manual playthrough verification + back-compat shim cleanup. User chose to stop here for the session. PR not opened yet. Resume by dispatching T13 (Engineer Dax) via subagent-driven-development, working from the plan at `docs/superpowers/plans/2026-05-30-hero-roster.md`.

---
## 2026-05-30 — Backlog hygiene + notes.md refresh

**Accomplished:** Added two backlog items — (1) verify hero is blocking enemies on Level 2 (Lunar Gate); (2) responsive Phaser canvas that resizes with the browser window. Took two wrong turns getting them filed: first created a standalone `docs/BACKLOG.md` (user pushed back: "aren't backlogs part of notes?"), then misread "notes" as `SESSION_NOTES.md` and added a BACKLOG section there (user pushed back: "why are these not in the backlog?"). Final correct home: `.claude/notes.md` "Prioritized Backlog," entries 4 and 5 (post-refresh). Also did a full refresh of `.claude/notes.md` — Current Status, In Progress, Backlog, and Completed were all stale (still claimed Phase 8 PR was open); now reflects Phases 1–8 + 9a/9b/9c + dead-enemy cleanup merged, with `feature/hero-path-restriction` as active work.

**Decisions made:** "Notes" in this project = `.claude/notes.md`, NOT `SESSION_NOTES.md`. SESSION_NOTES.md is a session-by-session commit log; the Prioritized Backlog lives in `.claude/notes.md`. Wrote feedback memory pointing at notes.md so the next "add to backlog" goes straight to the right file; corrected the MEMORY.md index line.

**Where we left off:** Backlog correctly captured. Active branch `feature/hero-path-restriction` is 5 commits ahead with tests in place — no PR yet. Suggested next steps when user returns: open the PR for hero-path-restriction (which may also resolve the new "hero blocking on Level 2" backlog item), then pick up additional heroes / Phase 8b music curation / Phase 10 iOS, or address the responsive-canvas item.

---
## 2026-05-19 — Phase 7 Implementation (Meta & Persistence)

**Accomplished:** Executed the full Phase 7 plan via subagent-driven development — 10 tasks, each with a spec-compliance review and a code-quality review. Ran `review-plan` first (verified all line numbers/APIs/modifier contract against source; documented one accepted cosmetic spec §8.2 deviation — no overlay connector lines). Created worktree `feature/phase-7-meta-persistence` off `feature/phase-3-tower-system`; the Phase 7 spec+plan commits lived only on the stale `feature/barracks-soldier-rebuild`, so they were cherry-picked over. Delivered: `SaveManager` (versioned `lastlight_save` envelope, legacy-array migration, replaces/deletes `ProgressManager`); `data/upgrades.js` (13-node Command Doctrine catalog); `UpgradeManager` (purchase/refund-cascade/getModifiers/getNodeState); modifier threading through Hero/Tower/Barracks/Soldier/TowerPlacementManager (all optional `modifiers` args with `?? default` fallbacks); GameScene wiring (read modifiers once, scale kill gold, emit real hero start level, `_commitStats` on victory/defeat); meta UI in index.html + MapSelectScene; `UpgradeTreeOverlay`. 169 tests passing, build clean. Final whole-branch review returned "ready to merge" (no Critical/Important). Browser-verified end to end via Playwright: legacy migration, meta bar/chips, overlay render, purchase, 15★ threshold gate, refund cascade, modifier effects (gold 130→170, hero maxHP 150→200, start level 2), stats commit + re-entry guard. PR #7 opened → https://github.com/Kbtimko/tower-defense/pull/7 (commits 6c99fa5→0d6a34c).

**Decisions made:** Two issues caught mid-execution and fixed: (1) the plan's own Task 4 test calls `um.getPurchasedUpgrades()` but the plan's `UpgradeManager` reference code never defines it — implementer correctly added it as a thin delegate (a plan defect missed by review-plan's consistency check). (2) Code review found `_commitStats` made the pre-existing unlatched `EconomyManager` `game:defeat` emit harmful (double stats commit) — fixed with `if (this.over/won) return;` re-entry guards in `_onDefeat`/`_onVictory`. A Task 1 code-review "Critical" (broken build from dangling `ProgressManager` imports) was correctly judged a false alarm — expected intermediate state resolved by Task 2.

**Where we left off:** PR #7 open, awaiting review/merge into `feature/phase-3-tower-system`. Next: merge PR #7, then Phase 8 (Audio & Polish). The Phase 7 worktree at `~/.config/superpowers/worktrees/tower-defense/feature/phase-7-meta-persistence` is still present and can be cleaned up after merge.

---
## 2026-05-19 — Phase 7 Brainstorm, Spec & Plan

**Accomplished:** Full brainstorm of Phase 7 (Meta & Persistence) using the visual companion. Scoped it as "both": persistence hardening (versioned save schema) + a meta-progression upgrade tree. Wrote and committed the design spec (`docs/superpowers/specs/2026-05-19-phase-7-meta-persistence-design.md`, commit 5ef33d7) and the 10-task implementation plan (`docs/superpowers/plans/2026-05-19-phase-7-meta-persistence.md`, commit abb19e7). Plan is complete with full TDD code in every step and a self-review section. Also diagnosed two non-issues for the user: a `stream_idle_partial` "streaming stall" warning in the VSCode extension log (benign — the API stream paused; every stall recovered before the 5-min deadline) and a `File save operation completed undefined` log line (benign — successful save, trailing `undefined` is just a logged empty result arg).

**Decisions made:** Star economy = spent currency (30★ max in game, 45★ to buy the whole tree → forced specialization). Upgrade structure = a prerequisite tree, 3 branches (Command/Logistics/Arsenal), 13 nodes. Refund = per-node with cascade (refunding a node also refunds its purchased dependents). Three deepest nodes (cmd_elite, log_garrison, ars_overcharge) gated behind 15★ total earned. Upgrades accessed from MapSelectScene via a DOM overlay. New `SaveManager` replaces `ProgressManager` (versioned envelope `{version,maps,upgrades,stats}`, migrates the legacy bare-array save). `UpgradeManager.getModifiers()` produces a flat 11-field modifier object applied once per match in GameScene.create().

**Where we left off:** Spec + plan both committed and user-approved. A mid-session stall was diagnosed as a harmless redundant `Write` to the already-committed plan file after a context compaction (Write tool's read-before-write guard rejected it) — no work lost. Next: run the `review-plan` skill on the plan file, then execute via `superpowers:subagent-driven-development` in a worktree branched off `feature/phase-3-tower-system`. Current branch is the stale `feature/barracks-soldier-rebuild` — Phase 7 work must NOT happen there.

---
## 2026-05-19 — Phase 6: Hero Unit (Commander Rael)

**Accomplished:** Full Phase 6 implementation via subagent-driven-development (7 tasks, 2 review stages each). Enemy stun status effect (timer, white ring visual, full-freeze skip in _updateEnemies). Hero.js entity: click-to-move at 130px/s, auto-attack 40px range, 18dmg/1.5rate, respawn 20s, leveling at 25/75 kills, 3 abilities (Overcharge Q: 30s CD +50% fireRate 6s; Airstrike W: 25s CD AoE r70 d80; EMP E: 45s CD stun all 3s). HTML hero section in bottom bar (portrait, HP bar, Q/W/E buttons with lock/cooldown states). GameScene wired: _updateHero, _applyOvercharge, _onPointerDown input disambiguation, _onAbility, _triggerAirstrike. UIScene wired: hero events, Q/W/E keyboard shortcuts, ability button unlock. Review fixes: level guard on W/E keyboard shortcuts, hero kills award gold, cooldown accumulator drift, aimMode stuck on airstrike early-return, dead-hero moveTo guard. 142 tests. PR #6 merged into feature/phase-3-tower-system. Commits: fb1698e → 84915b8.
**Decisions made:** Stun is a full freeze (skips both movement AND melee combat, not just movement — documented with comment). Overcharge restores exact fireRate via _baseFireRate stash on tower objects. Hero kill gold/kills awarded via alive-before/alive-after snapshot in _updateHero. airstrike kills (via _dealDamage) do NOT count toward hero level-up kill count — by design omission (not spec'd). Q button starts enabled at L1 via 150ms delayedCall after UIScene is ready.
**Where we left off:** Phase 6 merged. Up next: (1) manual playthrough to verify phantom rings (Map 5+) and titan hexagons (Map 6+); (2) Phase 7 brainstorm (Meta & Persistence).

---
## 2026-05-18 — Runtime Bug Fixes (Map Load)

**Accomplished:** Diagnosed and fixed three bugs blocking map load. (1) Phaser 3.60+ no longer auto-calls `shutdown()` as a lifecycle method — fixed by adding `this.events.on('shutdown', this.shutdown, this)` in `create()` for MapSelectScene, GameScene, and UIScene. This was the root cause of #map-select not hiding when Play was clicked. (2) `Enemy` and (3) `Projectile` constructors were refactored to take `(scene, options)` when Entity Containers were introduced, but `_spawnEnemy` and `_updateTowers` in GameScene were never updated — both now pass `this` as the first arg. Map 1 (Outpost Sigma) confirmed loading in Playwright after fixes. Changes are not yet committed.
**Decisions made:** All three scenes need explicit `this.events.on('shutdown', ...)` registration — this is a Phaser 3.60+ breaking change that silently dropped the auto-binding. UIScene is a legacy parallel-scene architecture from Phase 3 that coexists with the Phase 5 GameScene; both need the fix.
**Where we left off:** Three files edited (MapSelectScene.js, GameScene.js, UIScene.js), changes uncommitted. Next: commit the fixes, then continue browser validation of PR #4 (story banners, star rating, phantom/titan visuals, localStorage).

---
## 2026-05-17 — Phase 5 Implementation

**Accomplished:** Executed full Phase 5 implementation via subagent-driven-development (11 tasks, 2 review stages each). Data layer: phantom + titan enemy defs, 10 map definitions with blurbs/storyKeys, MAP_WAVES[0–9] (wave counts 10–18, phantom introduced at key 3, titan at key 5), phantom/titan visual shapes in Enemy._redrawBody, ProgressManager (localStorage, isUnlocked), starsDisplay utility. UI layer: STORY_PANELS for all 10 maps, StoryManager (CSS slide-in banner, dismiss handler), MapSelectScene (sidebar + featured panel, auto-selects highest unlocked), MenuScene redirects to MapSelectScene, GameScene wired (ProgressManager, StoryManager, star rating at 80%/50% thresholds, story banner between waves, msg-btn navigates to MapSelectScene). Fixed MAP_WAVES[1] gap found in final review (acc16c8). 116 tests passing across 9 test files. PR #4 open at https://github.com/Kbtimko/tower-defense/pull/4 targeting feature/phase-3-tower-system.
**Decisions made:** MAP_WAVES[1] added (was silently falling back to key 0 — players would notice identical waves). Dual-rendering concern (GameScene._drawEnemies vs Enemy._body) confirmed false alarm: Enemy Containers at depth 3, gfx at depth 0.
**Where we left off:** PR #4 awaiting browser validation. Checklist: MapSelectScene unlock flow, story banner slide-in, stars overlay on victory, phantom rings on Map 5+, titan hexagons on Map 6+, localStorage persistence across reload.

---
## 2026-05-17 — Session Summary
**Accomplished:** Marked PR #2 (Phase 4) and PR #3 (Barracks rebuild) as merged. Ran full brainstorm for Phase 5: Maps & Storyline using the visual companion (3 design questions shown in browser). Key decisions: localStorage for persistence, Sidebar + Featured Panel map select layout, slide-in banner for story panels, 2 new enemy types (phantom + titan), 80%/50% star thresholds, placeholder story text, two-sub-phase execution (5a data, 5b UI). Wrote and committed full design spec: 10 map defs with waypoints, MAP_WAVES[0-9] with complete wave arrays, phantom + titan ENEMY_DEFS and visual shapes, ProgressManager API + tests, MapSelectScene DOM structure and behavior, StoryManager API + STORY_PANELS structure + GameScene integration. Spec self-reviewed and two consistency issues fixed (getUnlockPanel separate from getPanelForWave, starsDisplay as shared utility).
**Decisions made:** titan armor = 20 flat (consistent with existing takeDamage mechanic, not percentage). colossus stays in ENEMY_DEFS but unused. MapSelectScene uses DOM (matching existing panel pattern). msg-btn navigates to MapSelectScene on both victory and defeat; retry out of scope. starsDisplay utility exported from src/utils/display.js.
**Where we left off:** Spec committed at docs/superpowers/specs/2026-05-17-phase-5-maps-storyline-design.md. User reviewed and approved spec. Next: write implementation plan (writing-plans skill), then execute via subagent-driven development.

---
## 2026-05-14 — Session Summary
**Accomplished:** Full Barracks/Soldier system rebuild (lost in a Phase 2 git restore). Implemented 11 commits across `feature/barracks-soldier-rebuild`: restored `soldierStats` nested object on barracks def (tier1–tier4B); added `PathManager.getPathPoints()` and `getNearestPathProgress()` (TDD, 4 tests); fixed `Tower.upgrade()` tier4A/tier4B key resolution; created `Soldier.js` (HP bar, respawn timer, melee attack) and `Barracks.js` (extends Tower, spawns/repositions/rebuilds soldiers); wired GameScene (Barracks placement dispatch, `_checkSoldierBlock` loop, `_updateSoldiers`, reposition mode with cyan overlay); refactored UIScene (Barracks stats panel with 5 soldier spans, branch picker via `createElement`, reposition button, proper `_onBarracksReposition` method for event deregistration, `unlockMap` fix). Two code quality fixes during review: `btn.disabled` in upgrade button, `nextLevel <= 3 ? 3 : 5` heuristic. All 33 tests pass. Browser-verified via Playwright + window.__game: soldiers appear, blocking/respawn confirmed, T1→T4 upgrades, branch picker locked correctly, reposition moves soldiers, sell cleans up. PR #3 open at https://github.com/Kbtimko/tower-defense/pull/3.
**Decisions made:** Skip TowerPlacementManager (not needed for soldier mechanic); GameScene orchestrates blocking (Option B, Enemy stays clean); soldierStats as nested object keyed by tier; branch picker included in this PR since it touched the same UIScene function.
**Where we left off:** PR #2 (Phase 4) and PR #3 (Barracks rebuild) both open and ready to merge. Next: merge both PRs, then Phase 5 (Maps & Storyline).

---
## 2026-05-12 — Session Summary
**Accomplished:** Full Phase 4 implementation — alien enemy system for Map 1 (Outpost Sigma). Created `enemies.test.js` and `waves.test.js` (38 tests total, all passing). Added `type` field to all enemy defs with alien color palette. Replaced `makeWaves()` with `MAP_WAVES` keyed object (10 balanced waves, Colossus removed). Implemented type-dispatched alien shapes in `Enemy._redrawBody()` (green hex drones, orange diamond skitters, grey-green layered brutes). Added death particle burst on kill reusing existing `_addParticle` system. Final review caught and fixed: MAP_WAVES fallback guard for Lunar Gate, shape-matched slow rings, hardcoded color literals replaced with `def.color`. Also fixed a pre-existing bug where only 4 of 6 towers appeared in dev (sniper and barracks defs were missing from towers.js, committed on main). PR #2 open at https://github.com/Kbtimko/tower-defense/pull/2.
**Decisions made:** Scoped Phase 4 to Map 1 only (no Skitter erratic movement, no new behaviors — visuals + wave balance + particles only). Brute armor base kept as hardcoded `0x334433` intentionally darker than `def.color`. MAP_WAVES fallback uses nullish coalescing (`?? MAP_WAVES[0]`) rather than erroring. towers.js fix committed directly to main (not feature branch) since it was a pre-existing bug unrelated to Phase 4.
**Where we left off:** Phase 4 PR #2 needs browser testing then merge. Barracks/Soldier system is missing (lost in Phase 2 restore) — needs a dedicated rebuild phase. Next up is Phase 5: Maps & Storyline.
